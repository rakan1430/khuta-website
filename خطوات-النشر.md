# خطوات النشر — افعلها بالترتيب حين يفتح رصيد Netlify

هذا الملف مكتوب لتنفَّذ خطواته بالترتيب دون الحاجة لفهم الكود.
كل خطوة تنتهي بـ**كيف تتأكد أنها نجحت**، فلا تنتقل قبل التأكد.

⏱️ الوقت المتوقّع: نحو ٤٠ دقيقة.

---

## قبل أن تبدأ — ما الجاهز فعلاً

| العنصر | الحالة |
|---|---|
| الكود كله | ✅ مرفوع على الفرع |
| قاعدة البيانات وجداولها وصلاحياتها | ✅ مطبَّقة ومختبَرة |
| دلاء التخزين (الملفات وصور الأسئلة) | ✅ مُنشأة بسياساتها |
| مزوّد Google في Supabase | ❌ **يحتاجك** (خطوة ٢) |
| متغيّرات البيئة على Netlify | ❌ **يحتاجك** (خطوة ٣) |
| أول حساب إداري | ❌ **يحتاجك** (خطوة ٤) |

---

## ١) اربط المستودع بموقع Netlify

في لوحة Netlify → الموقع `motaqadima-khuta` → **Site configuration → Build & deploy → Link repository**
واختر `rakan1430/khuta-website` والفرع المطلوب.

**تأكّد:** ظهور نشرة (deploy) جديدة حالتها *Published*.

---

## ٢) فعّل الدخول بحساب Google في Supabase

المشروع: `squhkiwjwwyrgufkaujf` → **Authentication → Providers → Google → Enable**.
ستحتاج `Client ID` و`Client Secret` من Google Cloud Console، وتضيف في
**Authorized redirect URIs** هناك:

```
https://squhkiwjwwyrgufkaujf.supabase.co/auth/v1/callback
```

ثم في Supabase → **Authentication → URL Configuration** أضف عناوين الموقع
في *Redirect URLs* (العنوان الأساسي وعنوان المدرسة إن اختلف).

> ⚠️ بدون هذه الخطوة **لا يعمل شيء في المدرسة إطلاقاً** — لا دخول الطلاب،
> ولا رمز QR، ولا تأكيد المعلّم. ابدأ بها قبل غيرها.

**تأكّد:** افتح الموقع واضغط "المتابعة بحساب Google" — يجب أن تصل لصفحة
اختيار الحساب من Google لا لرسالة خطأ.

---

## ٣) اضبط متغيّرات البيئة على Netlify

**Site configuration → Environment variables**:

| المتغيّر | القيمة |
|---|---|
| `SCHOOL_SUPABASE_URL` | `https://squhkiwjwwyrgufkaujf.supabase.co` |
| `SCHOOL_SUPABASE_SERVICE_ROLE_KEY` | مفتاح `service_role` من Supabase → Settings → API |
| `BREVO_API_KEY` | موجود أصلاً لخُطى — تأكّد فقط من وجوده |

> ⚠️⚠️ **انتبه لهذه تحديداً:** القيمة الحالية لـ`SCHOOL_SUPABASE_URL` تشير
> إلى المشروع القديم `gwbhwshxvlagmoonhfha` الذي صار فارغاً بعد الدمج.
> والخطر أن هذا **لا يُنتج أي رسالة خطأ**: الدالة تعمل، وتجد الطابور فارغاً
> دائماً، فلا يصل بريد أحداً ولا تعرف أنت السبب. صحّحها يدوياً.

> ⚠️ مفتاح `service_role` يتجاوز كل الصلاحيات. لا يوضع إلا هنا، ولا يُلصق في
> رسالة أو ملف أو محادثة أبداً.

**تأكّد:** بعد الحفظ اطلب **Trigger deploy → Clear cache and deploy site**
(متغيّرات البيئة لا تسري على نشرة قديمة).

---

## ٤) أنشئ أول حساب إداري

هذه أصعب خطوة لأنها بيضة ودجاجة: الإدارة تُضاف من لوحة الإدارة، ولا لوحة
بلا إداري. فأول إداري يُضاف يدوياً مرة واحدة فقط.

1. اطلب من الشخص الإداري أن يفتح الموقع ويسجّل دخوله **بحساب Google** مرة.
2. في Supabase → **SQL Editor** نفّذ (بعد تبديل البريد واسم المدرسة):

```sql
-- ١) أنشئ المدرسة إن لم تكن موجودة
insert into schools (slug, name_ar)
values ('motaqadima', 'مدارس المتقدمة — فرع الملقا')
on conflict (slug) do nothing;

-- ٢) اجعل هذا البريد إدارياً فيها
insert into school_members (school_id, uid, email, role, full_name, active)
select s.id, u.id, u.email, 'admin', 'اسم الإداري', true
from schools s, auth.users u
where s.slug = 'motaqadima'
  and u.email = 'ضع-بريد-الإداري-هنا@gmail.com'
on conflict do nothing;
```

3. اطلب منه تحديث الصفحة — يجب أن يظهر له قسم **إدارة المدرسة**.

**تأكّد:** ظهور "إدارة المدرسة" في قائمة التنقّل عنده.

بعدها يضيف بقية الإداريين والمعلّمين والطلاب من اللوحة نفسها، ولن تحتاج
SQL مرة أخرى أبداً.

---

## ٥) الروابط التي تسلّمها للمدرسة

| لمن | الرابط |
|---|---|
| الطلاب | `https://<الموقع>/` |
| المعلّمون (تقديم طلب) | `https://<الموقع>/?apply=teacher` |
| الإدارة | `https://<الموقع>/?panel=molqa-2026` |
| سبورة الفصل (يظهر فيها زر QR) | `https://<الموقع>/?board=1` |

> ⚠️ **صراحة عن رابط الإدارة:** كونه غير معلَن ليس حمايةً بحدّ ذاته — من رآه
> مرة حفظه. قيمته أنه لا يُعثر عليه بالتصفّح العادي فقط. الحماية الحقيقية
> كلمة المرور وصلاحيات قاعدة البيانات. ولو تسرّب فغيّر `ADMIN_PANEL_KEY` في
> `js/20-entry.js` — ولن يُبطل ذلك أي حساب.

---

## ٦) اختبار قبول سريع (١٠ دقائق)

نفّذها بنفسك قبل أن تعرضها على المدرسة:

- [ ] طالب يسجّل دخوله بـGoogle ويرى قسمه — **دون أن يحدّث الصفحة يدوياً**
- [ ] معلّم يرفع ملفاً (٢٥ ميجا حدّ أقصى) ويفتحه
- [ ] معلّم يضيف رابطاً، ويتأكد أن العاشر مرفوض (الحد ٩)
- [ ] معلّم ينشئ سؤالاً **بصورة** وخيارات صور ويحفظه
- [ ] معلّم يرسل الاختبار لفصل ولمرحلة ولطالب باسمه
- [ ] طالب من الفصل يرى الاختبار، وطالب من خارجه لا يراه
- [ ] معلّم يفتح "النتائج" بعد محاولة طالب
- [ ] على السبورة: `?board=1` → زر QR → المسح بالجوال → كتابة الرقم → تفتح
- [ ] بعد فتح السبورة برمز QR: زر الحذف **يجب أن يُرفض** ويظهر شريط "جلسة محدودة"
- [ ] الضغط على "تأكيد بحساب Google" يرفع الصلاحية ويعيدك لنفس الصفحة
- [ ] بعد إرسال اختبار: وصول بريد للطالب خلال ١٥ دقيقة (الدالة تعمل كل ربع ساعة)

---

## ٧) بعد نجاح كل ما سبق

- احذف مشروع Supabase القديم `gwbhwshxvlagmoonhfha` أو أوقفه — صار فارغاً
  وبقاؤه يربك من يأتي بعدك.
- راجع `التكاليف.md`: الخطة المجانية في Supabase **بلا نسخ احتياطي**.
  أنصح بالترقية عند توقيع العقد لا بعد امتلاء المساحة.
- ضع حدّ إنفاق (Budget Alert) في Google Cloud قبل تفعيل الذكاء الاصطناعي.

---

## إن تعطّل شيء

| العَرَض | الأرجح |
|---|---|
| "تعذّر الاتصال بالخادم" في كل مكان | مزوّد Google غير مفعّل (خطوة ٢) |
| الدخول يعمل لكن لا تظهر أقسام المدرسة | الحساب غير مضاف كعضو (خطوة ٤) |
| رفع الملفات يفشل | المعلّم في جلسة محدودة — يضغط "تأكيد بحساب Google" |
| لا يصل بريد الاختبارات | `SCHOOL_SUPABASE_URL` خاطئ (خطوة ٣) |
| رمز QR يظهر ثم لا يحدث شيء | `SCHOOL_SUPABASE_SERVICE_ROLE_KEY` ناقص (خطوة ٣) |

---

## ملحق: الرابط المميّز للملف

لكل ملف يرفعه المعلّم رمز خاص، وزرّ 🔗 بجانبه ينسخ رابطاً مثل:

```
https://<الموقع>/?f=saqkk3e
```

> ⚠️ الرمز **اختصار لا مفتاح**: من يفتح الرابط يخضع لصلاحياته كاملةً. فلو
> أعطاه المعلّم لطالب من فصل آخر لم يفتح له شيء، ولو ألغى المشاركة توقّف
> الرابط فوراً. الرابط يوفّر البحث فقط ولا يمنح أحداً ما لا يحق له.
