# سجل المشروع — يُقرأ قبل كل ردّ

> 🔴 **إلى المساعد: اقرأ هذا الملف كاملاً في بداية كل جلسة وقبل كل ردّ فيه
> قرار.** أُنشئ بطلب صريح من المالك بعد أن نسيتُ وصفاً سبق أن أعطانيه
> وأعدتُ سؤاله من الصفر. قال: «ذاكرتك لا يمكن الاعتماد عليها» — وكان محقاً.
>
> **وحدِّثه في نهاية كل خطوة**، ولو بسطر. أي تفصيلة تبدو صغيرة اليوم قد
> تكون هي المفتاح بعد عشرين رسالة.

**آخر تحديث:** ٢٦ سبتمبر ٢٠٢٦

## 🔗 رابط المعاينة الثابت — قاعدة دائمة من الآن (٢٥ سبتمبر)

> المالك: «المفترض انك ترفعه على رابط المعاينه الذي كنا نستعمله… لا ان تنسى
> رابط معاينه كل مره وتسبب ازدحام روابط جديده».

**المشكلة:** رابط كل طلب دمج (Pull Request) يرتبط برقمه (`deploy-preview-25`،
ثم `-26`، ثم `-27`…)، ويتجمّد بلا تحديث بمجرد إغلاق الطلب أو دمجه. فكل طلب
جديد بعد كل دمج يعني رابطاً جديداً — وهذا ما رفضه المالك بحقّ.

**الحل: رابط فرع ثابت (Branch deploy) لا يتغيّر أبداً**، مفعَّل الآن في
Netlify (Site configuration → Build & deploy → Branches and deploy contexts)
للفرع `claude/system-security-audit-xijbr6`، ومُضاف في Supabase
(Authentication → URL Configuration → Redirect URLs) — الاثنان أضافهما
المالك بنفسه ٢٥ سبتمبر.

```
https://claude-system-security-audit-xijbr6--khutaa.netlify.app
```

**من الآن فصاعداً:**
- **كل ميزة جديدة تُرفع لهذا الرابط أولاً** — بمجرد الدفع (push) للفرع، بلا
  حاجة لفتح أي طلب دمج جديد ولا انتظار رقم رابط جديد.
- لا يُذكر أي رابط `deploy-preview-N` للمالك بعد اليوم كرابط تجربة.
- الرفع للموقع الأساسي (`khutaa.netlify.app`) يبقى كما هو: **دفعة واحدة
  بأمر صريح من المالك**، لا أوتوماتيكياً.
- طلبات الدمج (Pull Requests) تبقى تُفتح للتوثيق والدمج النهائي فقط — لا
  لأجل رابط معاينة، فالرابط الثابت لا علاقة له برقم الطلب ولا بحالته.
- ⚠️ **لا تنشئ رابط معاينة جديد بعد اليوم لأي سبب.** إن بدا الرابط الثابت
  معطوباً فالسبب غالباً بناء لم يكتمل بعد أو إعداد ناقص — يُفحص ويُصلح، لا
  يُستبدل.

## 🚀 أُطلقت الدفعة على الموقع الأساسي (٢٤ سبتمبر) — بإذن المالك
> «هل انتهينا من كل الميزات الجديده؟ اذا نعم فابدا في الرفع على khutaa.netlify.app
> … وبعد الرفع قم بفحص شامل والتاكد حتى من المشاكل البصريه»

- دُمج PR #25 في `main` (commit `96224c7`) — المراحل ٠–٥ + ملاحظات المالك + الكتابة بخط اليد.
  نشر Netlify للإنتاج جاهز (16 دالّة منها `sensitive-code`، 7 قواعد ترويسات، فحص الأسرار نظيف).
  المتغيّرات السرّية التي تحتاجها الدوالّ الجديدة كلها مستعملة أصلاً في الإنتاج — لا جديد يُضبط.
- ما بقي خارج الدفعة **عمداً** (بقرار الخطة): PDPL «لاحقاً»، والتحصيلي «قريباً».
- بعد الإطلاق: فحص الصلاحيات (بلا صفوف) · مستشار Supabase (المعروف فقط: حماية كلمات
  المرور المسرّبة مطفأة، ودوالّ SECURITY DEFINER مقصودة) · سجلات الشبكة بعد النشر كلها 2xx.
- **الفحص البصري الشامل** `tools/فحص-بصري-شامل.js` (جديد): كل تبويب لكل دور × جوّال/حاسوب ×
  فاتح/داكن، + النوافذ المنبثقة، مع ضابطٍ يزرع عيوباً يجب أن يلتقطها. ما وجده وأُصلح:
  1. **أبيض على ذهبي فاتح في الوضع الداكن** (1.63:1): رؤوس أيام الجدول، زرّ الدور النشط،
     تبويب شرح الحل النشط — كانت خلفيتها `--gold-text` (لون نصّ يصير فاتحاً في الداكن)؛
     صارت `--gold-btn` (4.56:1 في الوضعين).
  2. **عنوان الواجب/الاختبار منضغط** على الجوّال لكلمة في كل سطر بجانب الأزرار: أساس المرونة
     كان صفراً فلا تنزل الأزرار لسطر جديد. `.sfile-main{flex:1 1 220px}` — يشمل كل قوائم
     الملفات والروابط والاختبارات.
  3. متغيّر `--text` غير معرَّف أصلاً (شريط الجلسة المحدودة وحاوية أخرى) → `--text-1`.
  4. **زرّ فتح القائمة الجانبية نصفه خارج الشاشة** على الحاسوب حين تكون مطويّة (الافتراضي):
     كان على حافة الحاوية = حافة الشاشة في أي شاشة أضيق من 1560px. صار داخلها (2px) بلا
     تداخل مع بطاقة الترحيب، وفوق شريط الفتح الجانبي.
  5. 🔴 **شريط «دخول سريع» يغطّي رأس النوافذ على الجوّال** — الأخطر بينها: نافذة إرسال
     الاختبار (أزرار فصول/مراحل/طلاب) ونافذة النتائج (العنوان والنصّ) كانت مكتوبة داخل
     `.main-content` (z-index:1 = سياق تكديس يحبسها تحته مهما علا رقمها 3000). الآن تُنقل
     كل `.overlay-screen` داخلها إلى body عند الإقلاع (`js/05-boot-nav.js`). يصيب كل معلّم
     دخل بالدخول السريع — وهو الشائع.
  - الفحص يتحقّق الآن أن كل عنوان ونصّ وزرّ في النافذة المفتوحة هو فعلاً ما يقع تحت مركزه
    (`elementFromPoint`) — مُثبَت: بلا الإصلاح يفشل في ٤ نوافذ، وبه ينجح.
    `SWEEP_ONLY=overlays` يفحص النوافذ وحدها في دقيقتين.
  - كل أدوات الفحص الأخرى ٢١/٢١ ناجحة بعد الإصلاحات · `sw.js` ← v42.
  - وأخطاء في الفحص نفسه صُحّحت قبل الوثوق به: كان يقيس `<main>` شاشة الاختبار المخفيّة؛
    ويُعفي المحتوى المقصوص بـoverflow:hidden؛ ويقرأ `color(srgb …)` كأنها 0..255.

## ✉️ رسائل البريد: لمن تصل ومن يرسلها + إصلاح إشعار الجوال (٢٥ سبتمبر) — على المعاينة
> المالك: «خانة في إعدادات المشرف ليختار أن تصل لطلاب منصّة المدرسة فقط أو طلاب خُطى فقط
> أو الكل… الإدارة لدى المدرسة لا يظهر لها سوى إرسال لطلاب المدرسة، ويمكن جدولتها أن
> تُرسل تلقائياً… ولديّ يظهر الاثنان ويمكنني إدارتهم ووصول الإشعارات لديهم».

- **القاعدة** (`sql/PHASE8_MESSAGES.sql`، طُبّقت — إضافية، الإنتاج بلا تغيير): أعمدة
  `audience/school_id/origin/body_format/created_by/sending_started_at/send_result` على
  `marketing_messages` (قيمها الافتراضية = السلوك القديم حرفياً)، وجدول `notify_optouts`
  (scope school|khuta، source self|owner)، و`campaign_pool` مصدر واحد لـ«من يستلم»:
  **رسالة خُطى لمن وافق فقط** (طالب مدرسة أو غيره)، **رسالة المدرسة لطلابها النشِطين** إلا من
  أوقفها، و«طلاب خُطى» = ليسوا أعضاء نشِطين في مدرسة، ولا مجهول ولا بريد مصطنع أبداً.
  إدارة المدرسة (سياسات جديدة): ترى رسائل مدرستها فقط، وتكتب لطلابها نصّاً عادياً فقط،
  بدخول Google حديث، ٣ في اليوم؛ ومحفّز يمنعها من كتابة حالة الإرسال. المالك يوقف/يعيد
  لأي طالب، و**لا يرفع إيقافاً وضعه الطالب بنفسه**. مُختبَر بمدير مدرسة ليس مالكاً (رُقّي
  مؤقتاً داخل معاملة تراجعت)، وبلا Google، وطالب، والمالك، والخادم — كل النتائج كما يجب.
- **الإرسال** (`send-email.js`): المستلمون من القاعدة لا من المتصفّح؛ قفل مشروط ضد الإرسال
  المزدوج؛ دفعات متوازية (٨) وسجل بنداء واحد (مهلة الدالّة ثوانٍ)؛ حصّة يومية
  `BREVO_DAILY_CAP` (افتراضي ٣٠٠ = خطة Brevo المجانية) — رسالة تتجاوزها لا تُرسل ويُقال
  السبب؛ رسالة المدرسة نصّ مُهرَّب واسم المدرسة في العنوان ورابط «إيقاف رسائل المدرسة»
  بتوقيع مستقل (`unsubscribe.js?scope=school`). مسارات جديدة: `schoolTest` (لبريد المدير
  نفسه) و`schoolSendCampaign` و`scheduledRun`.
- **الجدولة**: `send-campaigns.js` كل ربع ساعة يطرق `send-email` بتوقيع HMAC (نافذة ٥ دقائق)
  — منطق إرسال واحد للزرّ وللمجدول.
- **الواجهة**: لوحة المالك — «لمن تصل» (الكل/خُطى/المدرسة + أيّ مدرسة) مع عدد الواصلين،
  وموعد بالساعة، وتبويب «المستلمون» (أرقام الفئتين، بحث، إيقاف/إعادة). إدارة المدرسة —
  بطاقة «رسائل البريد لطلاب المدرسة» (`js/41-school-messages.js`، مجموعة school الكسولة).
- 🐞 **إشعار الجوال اليومي لم يعمل قطّ**: `send-reminders.js` كان يعتمد web-push وsupabase-js
  ولا يثبّتهما Netlify (لا package.json في الجذر) فيسقط عند التحميل — `job_runs` فارغ منذ
  يوليو، وحجم حزمته كالدوالّ بلا مكتبات. ⚠️ **ظننتُ أوّلاً أن `VAPID_PRIVATE_KEY` ناقص وقلتها
  للمالك — كان خطأً**: تحقّقتُ من Netlify فوجدته مضبوطاً منذ ٢٤ يوليو. أُعيدت الدالّة بـcrypto
  وfetch فقط: إشعار بلا محتوى (sw.js يعرض النصّ) وتوقيع VAPID ES256، وتتحقّق أن المفتاح
  الخاص يطابق العام وتقولها صريحةً في السجل إن لم يطابق. `sw.js` ← v43.
- 🐞 **درس**: قلتُ في الورقة التعريفية إن «الاختبارات المحاكية مخفية» — نظرتُ في HTML
  (`style="display:none"`) ولم أتتبّع `FEATURE_EXAM_SIMULATOR = true` الذي يُظهرها. صُحّح.
  **القاعدة: لا تُوصَف ميزة من حالتها الابتدائية في HTML؛ تتبّع الكود الذي يغيّرها.**
- 🐞 مفاتيح ترجمة البطاقة الجديدة ظهرت نصّاً خاماً (`smsg.title`) — أُضيفت، وصار فحصٌ دائم
  أن كل `data-i18n` (٦١٨ مفتاحاً) له عربي وإنجليزي.
- الفحوص الجديدة: `فحص-رسائل-البريد.js` ٢٨ · `فحص-رسائل-المدرسة.js` ٣١ ·
  `فحص-إشعارات-الجوال.js` ١٧ — وكل الأدوات ٢٤/٢٤ · فحص صلاحيات الدوال نظيف.
- ⚠️ ما يُتأكَّد منه بعد الدمج فقط: أوّل تشغيل لـ`send-reminders` (٦ مساءً) في سجل دوالّ Netlify
  — «تم الإرسال: N» أو رسالة عدم تطابق المفتاح.

## 🎨 ملاحظات المالك البصرية قبل الرفع للأساسي (٢٦ سبتمبر) — على الرابط الثابت، لم يُدمج
> المالك بعد تجربة المساعد («النتيجة ممتازة جداً وأعلى مما كنت أتخيل»): «قبل الرفع… لاحظت
> بعض المشاكل البصرية البسيطة… فقط اعمل على التفاصيل التي ذكرتها لك الآن». **لا دمج بلا إذن.**

1. **تداخل ووميض عند التمرير (جوال وآيباد)** — لا JS تمرير في الموقع إطلاقاً؛ السبب رسومي:
   `backdrop-filter` على ٤٠+ نوع عنصر (كل بطاقة وكل حقل)، وطبقتا نجوم مثبّتتان تومضان بلا توقف
   تحتها (فيُعاد حساب ضبابية كل بطاقة كل إطار)، وزخارف عائمة مثبّتة خلف بطاقات شبه شفافة — كانت
   مخفية تحت ٧٦٨px فقط، **فظهرت على الآيباد** تنزلق خلف الحقول (هذا «التداخل»؛ رأيتها في لقطة
   ١٦٠٠px: القلم فوق حقل اللقب والكتاب فوق حقل التاريخ). العلاج `html.touch-ui` (سكربت في
   `<head>` قبل أول رسم؛ الآيباد يُعرف بنقاط اللمس لأنه يقدّم نفسه Mac): لا ضبابية، لا زخارف،
   نجوم ثابتة، وأسطح مصمتة **محسوبة من لون الزجاج فوق المجرّة** (‎#292153→#19133D‎) فلا يتغيّر
   الشكل. الحاسوب بلا تغيير. ⚠️ لا يمكن اختبار وميض Safari هنا — يُتأكَّد منه على جهاز المالك.
2. **السبورة على الجوال بلا X** — المعلّم تُفتح له ملء الشاشة `100vw×100vh` داخل خلفية لها حشوة
   ومحاذاة للأسفل، و`vh` على Safari أطول من الجزء الظاهر → الرأس (وفيه X) فوق الشاشة. الآن: ملء
   الشاشة تلقائياً على شاشة أعرض من ٩٠٠ فقط (سبورة الصف والحاسوب)، و`dvh` وسقف ارتفاع، وخلفية بلا
   حشوة حين تكون ملء الشاشة، وهامش المنطقة الآمنة. مُقاس في ٣ مقاسات: X داخل الشاشة ويغلق.
3. **اسم المنصة في الأعلى** — تحت شعار «خُطى.» صار «منصة المتقدمة» لكل عضو مدرسة (من
   `schools.name_ar` = «مدارس المتقدمة — فرع الملقا»)، وعنوان التبويب «خُطى — منصة المتقدمة»؛
   وطالب خُطى يبقى «رفيق القدرات». يُعدَّل مفتاح القاموس نفسه فلا يعيده تبديل اللغة.
4. **الساعة** — ساعات ودقائق **بلا ثوانٍ** (طلبها المالك أكثر من مرة — ⚠️ لا تُرجع الثواني)، في
   إطار ذهبي بخطّ العرض وأرقام جدولية، ونقطتان تنبضان بهدوء، وص/م صغيرة.
5. **التقويم** — كان رأس الصفحة هجرياً لأن `"ar-SA"` وحدها هجرية، بينما مواعيد الاختبارات ميلادية.
   الآن `khutaLocale()` في `js/04-utils.js` مصدر واحد، وكل عرض تاريخ في ١٧ ملفاً يمرّ منه
   (الرأس، الجدول وخريطة النشاط، مواعيد الاختبارات والواجبات، السجلّ، التقرير، الرسائل…).
   ميلادي افتراضاً، و«التقويم» في الإعدادات → هجري (أم القرى). ⚠️ حقول إدخال التاريخ يرسمها
   المتصفح بتقويمه ولا تتبع الإعداد. `parent.html` صفحة وليّ الأمر بقيت ميلادية (جهاز آخر).
6. **الصاروخ 🚀** بجانب الاسم — يُحذف للمعلّم والإدارة، ويبقى للطالب (مدرسياً أو خُطى).
7. **النص الباهت في خانة المساعد** — كان ثابتاً «افتح الواجبات…» للجميع؛ الآن يُبنى من الأقسام
   الظاهرة لصاحبه. ومن التدقيق المماثل: المعلّم/الإدارة لا يُعرض لهم جواب قدرات جاهز حين يتعطّل
   الذكاء، ولا تُذكر «صفحة الروابط» لمن لا يراها.
- الفحص: `tools/فحص-الرأس-والتقويم.js` **٥٧ ناجح**، وكل الأدوات ٢٧/٢٧ (البصري الشامل ١٦٢ شاشة، المربّع الأبيض ٠). `sw.js` ← v45.

## 🤖 المساعد الحيّ — يتحكّم في الصفحة لا يكتفي بالكلام (٢٦ سبتمبر) — على الرابط الثابت، لم يُدمج
> المالك: «الذكاء الاصطناعي لدى المعلم أو المدير يرفض شرح شي مثل كيف ارسل اختبار للطلاب او
> اصغ لي سوال اختبار من متعدد او اشرح هاذي المسأله على السبوره… ورفض ايضا الانتقال للقوائم
> الجديده… ليس مجرد زر خامد في زاوية الصفحه… اختبارات سريعه على السبوره، تغيير الوضع الداكن او
> الفاتح بشكل حي، فتح اي قائمه، شرح ميزه معينه بشكل حي على الموقع». **لا دمج حتى يأذن المالك.**

- **سبب الرفض** (في الخادم لا المتصفّح): `KHUTA_IDENTITY_CORE` في `gemini-proxy.js` يحصر النطاق
  في القدرات ويأمر برفض «واجبات مدرسية لمواد أخرى»، وقائمة أقسام التنقّل لم تضمّ أي قسم مدرسي.
  صحيح لطالب قدرات، خاطئ لمنصّة مدرسة. الآن `EDU_IDENTITY_CORE`: التعليم كلّه وعمل المدرسة
  والمنصّة («لا ترفض أبداً طلباً تعليمياً أو مدرسياً»)، وحماية الهوية كما هي حرفياً.
  السبورة والدفتر صارا لأي مادة. نمطا `chat` و`exam` بقيا كما هما (الأوّل لتوافق النسخ القديمة).
- **نمط `assistant` جديد**: التعليمات تُبنى لكل طلب من قطع ثابتة + سياق منقّى (الدور، القسم
  المفتوح، الأقسام الظاهرة له، المظهر) — كل قيمة من قائمة معروفة وإلا تُسقط، فلا يدخل نصّ حرّ من
  المتصفّح للتعليمات. `owner` لا يُقبل إلا لمشرف مُتحقَّق منه. الرد JSON مضمون من المصدر
  (`responseMimeType`): `{reply, actions[], chips[]}`.
- **`js/42-assistant.js`** (يُحمَّل دائماً — الزرّ في كل صفحة):
  - **أوامر فورية بلا ذكاء** — لحظية ولا تستهلك من الحصّة وتعمل للضيف: «فعّل الوضع الداكن»،
    «كبّر الخط»، «افتح الواجبات»، «وين الاختبارات»، «اشرح لي هذه الصفحة»، و«كيف أرسل/أنشئ
    اختباراً/واجباً» للمعلّم (شرح ثابت + جولة). المطابقة **بالكلمات كاملة**: «سورة الفاتحة»
    و«الطاقة الداكنة» و«حوّل النص للإنجليزي» تذهب للذكاء ولا تقلب المظهر ولا اللغة.
  - **الأفعال (قائمة بيضاء)**: فتح أي قسم ظاهر له (ومنشئ الاختبار/الواجب والسبورة والدفتر)،
    مظهر/لون/خط/لغة، **جولة حيّة** تظلّل الأزرار الحقيقية خطوة بخطوة (١٦ جولة + «هذه الصفحة»
    لأي قسم تُبنى من بطاقاته)، **شرح على السبورة**، و**اختبار سريع على السبورة** (سؤال كبير،
    أ/ب/ج/د، مؤقّت ١٥/٣٠/٦٠ أو بلا، «اكشف الإجابة»، السبب، النتيجة، ملء الشاشة للبروجكتر،
    لوحة المفاتيح ١–٤ وEnter)، و**أسئلة جاهزة** ← بطاقة: اعرضها على السبورة / أضفها لمنشئ
    الاختبار / انسخها بصيغة الاستيراد السريع. سبب الإجابة يصير «شرح الحل» في المنشئ.
  - **لا فعل يحفظ أو يرسل أو يحذف**: الأسئلة تدخل المسودّة فقط، والمعلّم يراجع ويحفظ ويرسل.
    والمتصفّح يعيد التحقّق من كل فعل: قسم مخفي عنه يُرفض بشارة «غير متاح لحسابك»، وجولات
    المعلّم ومنشئه مرفوضة للطالب، وHTML من النموذج يظهر نصّاً (مُختبَر بـ`<img onerror>`).
  - **الواجهة**: ترحيب واقتراحات لكل دور وقسم، «أنت في: …» في الرأس، شارات إنجاز تحت كل رد،
    اقتراحات تالية من الذكاء، **إدخال بالصوت** (عربي/إنجليزي) — لذلك صار
    `Permissions-Policy: microphone=(self)` بدل `()` في `netlify.toml`، وفقاعة «أول مرة في
    المكتبة؟ أوريك» عند أول زيارة لقسم (للمدرسة فقط، مرة لكل قسم، و«لا تقترح» يوقفها)،
    و`Ctrl/⌘+K` يفتح المساعد.
- 🐞 **وُجد أثناء البناء**: `normalizeArabic` مُعرّفة مرّتين (`js/12` ثم `js/27` التي تغلبها)،
  ونسخة `js/27` **تحذف المسافات** — فتصير «فعّل الوضع الداكن» كلمة واحدة. للمساعد تطبيعه
  الخاص `astNorm`. (مطابقة الأسئلة الشائعة القديمة لم تتأثر: الطرفان يُطبَّعان بالطريقة نفسها.)
- 🐞 **الجولة الأولى وضعت البقعة في مكان خاطئ**: قاست العنصر قبل انتهاء التمرير الناعم،
  وبطاقتها نزلت تحت شريط الجوال/الشريط التجريبي. الآن تنتظر استقرار العنصر، وتتبعه مع كل
  تمرير، وتحسب ارتفاع الأشرطة السفلية. رأيتها في لقطة قبل الفحص — فأُضيف للفحص ما يقيسها.
- الفحص: `tools/فحص-المساعد-الحي.js` **٨٥ ناجح** (الخادم بطلب كامل مزيّف الشبكة، الأوامر،
  الالتقاط الخاطئ، الجولة على الزرّ الحقيقي، المنشئ، الاختبار السريع، السبورة، الأمان، الطالب،
  الجوال بلا تمرير أفقي). `sw.js` ← v44.
- 🐞 جولتا «إدارة المدرسة» و«رسائل البريد» كانتا متاحتين لأي دور (والفقاعة تقترحها للطالب لو
  فُتح القسم برمجياً) — صارتا للإدارة فقط، والفقاعة لا تقترح قسماً لا يراه صاحبه.
- كل الأدوات ٢٦/٢٦ ناجحة، والفحص البصري الشامل ١٦٢ شاشة بلا فشل. ⚠️ `فحص-المربع-الأبيض.js`
  يلتقط أحياناً شريطاً بعرض الشاشة وارتفاع ٨–١٦ بكسل أعلى الشريط التجريبي — **متقطّع وموجود
  في النسخة السابقة أيضاً** (ظهر مرة من ثلاث تشغيلات على الكود قبل المساعد). لقطته لا تُظهر أي
  بياض؛ الأرجح توقيت ظهور الشريط التجريبي. لم يُطارَد بعد.
- ⚠️ **لم يُختبر بالذكاء الحقيقي بعد** — الفحوص تزيّف ردّه. أوّل ما يُجرَّب على الرابط الثابت:
  «صُغ لي ٥ أسئلة عن الكسور لثاني متوسط»، «اختبار سريع على السبورة عن …»، «كيف أرسل رسالة
  لطلاب المدرسة؟» (إدارة).

## 🚦 قاعدة النشر الجديدة — مُلزِمة (٢٣ سبتمبر)

> «كل شي ستقوم برفعه ارفعه على رابط المعاينه وليس على رابط خطى الاساسي
> … بعد اكتمال كل شي وكل المهام ساعطيك الاذن لرفع كل شي على رابط خطى
> الاساسي، لاكن الان كل شي على رابط المعاينه»

- **ممنوع الدمج في `main`** حتى إذن صريح من المالك بإطلاق الدفعة كاملة.
- العمل يُدفع للفرع، ويُفتح له PR **يبقى مفتوحاً** → رابط `deploy-preview-N--khutaa.netlify.app`.
- كل نقطة تُختبر على رابط المعاينة قبل الانتقال للتالية.
- **قلّل عدد الدفعات (push)**: كل دفعة تبني معاينة وتستهلك من رصيد البناء.
- ⚠️ **قاعدة البيانات واحدة للمعاينة والإنتاج.** أي هجرة على Supabase تصل
  الإنتاج فوراً. لذلك كل هجرة **إضافية فقط** (أعمدة/جداول/دوال جديدة) ولا
  تكسر ما تعتمد عليه الواجهة المنشورة حالياً.

## 🗺️ خطة «خُطى بديلاً عن "س ص" و"مزن"» — قرارات ٢٣ سبتمبر

المدرسة تريد خُطى بديلاً عن منصّتين عندها: **«س ص»** (مذاكرة القدرات)
و**«مزن»** (واجبات واختبارات وشروح مسجّلة). والمالك يريدها متوازنة للمدرسة
ولطلاب خُطى من خارجها، **في موقع واحد**، وقابلة للتوسّع لمدارس أخرى.

### الترتيب المعتمد

| المرحلة | المحتوى |
|---|---|
| **٠. الأساس** ← نبدأ هنا | لوحة المالك (إنشاء مدرسة + تعيين مديرها + حدود التخزين والذكاء والطلاب) · مراحل ومواد تضبطها كل مدرسة بدل «أول/ثاني/ثالث ثانوي» الثابتة · **ربط المعلّم بمادة وفصل** · روابط انضمام خاصة بكل مدرسة · تخفيف الثقل على **الجوّال فقط** |
| ١ | الواجبات + قاعدة بيانات المعلّم + سجل الدرجات |
| ٢ | مكتبة الكتب والدروس |
| ٣ | مساحة القدرات للمعلّم |
| ٤ | «ملفاتي» |
| ٥ | لوحة المرشد الطلابي |
| لاحقاً | نظام حماية البيانات (PDPL) — بعد التأكد من الأساس |

### القرارات بالتفصيل

**الواجبات**
- إلكترونية بأسئلة اختيارات — **لا** تسليم بالصور (رفض المالك فكرتي).
- الحل المشروح يظهر **بعد انتهاء المدة كاملة** لا بعد تسليم الطالب — لئلا يخبر زملاءه.
- **خلط ترتيب الأسئلة والخيارات لكل طالب** ✅ معتمد.
- من فاته الواجب **يرى الحل** بعد انتهاء المدة.
- التسليم المتأخر: خيار بيد المعلّم، **مطفأ افتراضياً**.
- تُبنى على محرّك الاختبار نفسه (`reveal_mode = after_due` موجود أصلاً).

**قاعدة بيانات المعلّم**
- يرى طلابه **في مادته هو** ودرجاتهم وواجباتهم واختباراتهم السابقة.
- **يُبنى الأساس كاملاً أولاً** (ربط معلّم↔مادة↔فصل) ثم الميزة.
- الدرجات الفصلية/النهائية تُستورد من **ملف تصدير نور** بنفس أسلوب الغياب.
- **لا حساب لمعدّل ولا لدرجة** — تُعرض كما هي من السجل الرسمي حرفياً.
- ⚠️ **هذا يعدّل قيد «بيانات نور» في القسم ٢**: المالك اعتمد صراحةً عرض
  الدرجات من ملف تصدير نور. يبقى الشرط: عرض لملف، لا ربط مع نور، ولا
  رقم هوية مخزَّن (بصمة HMAC فقط كالغياب).

**الكتب والدروس**
- ⚖️ **لا تُرفع نسخة الكتاب الوزاري على تخزين خُطى.** شروط منصة عين:
  «المناهج والكتب ملك حصري لوزارة التعليم… يُمنع منعاً باتاً إعادة نشر
  أو بيع أو استغلال…، ويُسمح فقط بالاستخدام الشخصي والتعليمي غير
  التجاري». الرفع لمنصة يصلها الطلاب = إعادة نشر. ومواقع «واجباتي»
  وأمثالها تنشر **حلولها هي** لا الكتاب ذاته.
- ✅ **معتمد: رابط منصة عين** للكتاب نفسه، وخُطى تحمل ما حوله فقط:
  دروس الكتاب، رابط فيديو لكل درس، اختبار لكل درس، ملفات المعلّم.
- ما يؤلّفه المعلّم بنفسه يُرفع بلا أي مشكلة.
- الكتاب ملك **المدرسة** لا المعلّم — يبقى لمن يخلفه وللسنة القادمة.

**السجل**
- لوليّ الأمر عبر الرابط الموجود، وللإدارة كاملاً في أي وقت.
- **سجل «من اطّلع على ملف الطالب ومتى»** ✅ معتمد.

**التحصيلي** — يبقى «قريباً». يحتاج جهداً كبيراً لتفرّعاته.

**مساحة القدرات للمعلّم**
- يرسل اختبارات قدرات وملفات وروابط فيديو لطلابه — **تدريب بلا درجات**.
- الطالب يعيد الاختبار متى شاء ويرى محاولاته السابقة.
- المحتوى **لطلاب المدرسة فقط**، لا يصل طلاب خُطى العامّين.
- المعلّم يرى **عدد المختبرين فقط** بلا أسماء ولا درجات ✅ معتمد.
- ❌ **ملغاة فكرة** أن يحفظ المالك محتوى المعلّمين لينشره لاحقاً لكل خُطى —
  بسبب حقوق المحتوى (يحتاج بنداً في الاتفاق مع المدرسة).

**«ملفاتي» لطلاب خُطى**
- **٣٠ ميجا** للطالب، لا تُتجاوز.
- **لحسابات Google فقط** ✅ معتمد نهائياً.
- PDF وصور فقط. **لا روابط مشاركة إطلاقاً** — لا يراه إلا صاحبه.
- فيه خانة «ملفات من خُطى» يرفعها المالك بعد فحصها (بلا شعارات لأحد).

**المرشد الطلابي** — ضعف الفصل **حسب المادة** (الرياضيات مثلاً) لا حسب
مهارات القدرات: القدرات في خُطى تنظيم وقت بلا دروس مقسّمة. يأتي آخراً
لأنه يحتاج بيانات فعلية متراكمة.

**ثقل الموقع**
- **لا فصل للموقع** — موقع واحد دائماً.
- التحميل عند الحاجة (لا يُحمَّل القسم إلا عند فتحه) — **على الجوّال فقط**.
- الكمبيوتر **والتطبيق المثبَّت** يحمّلان كل شيء كما هو اليوم.
- لا قياس الآن («الأوضاع لا تساعد») — نعتمد على الاقتراح مباشرة.

**PDPL** — المالك يرى أن خُطى تستوفي أغلب الشروط أو تحتاج تعديلاً بسيطاً.
يُناقش **بعد** اكتمال الأساس.

### ✅ المرحلة ٠ — أُنجزت (٢٣ سبتمبر) على رابط المعاينة، لم تُدمج في `main`

**رابط المعاينة:** `https://deploy-preview-25--khutaa.netlify.app` — PR #25 **مفتوح،
لا يُدمج** إلا بإذن المالك. كل مرحلة قادمة تُدفع لنفس الفرع فتتحدّث نفس المعاينة.
(البناء الأول فشل من Netlify نفسه — «Failed to fetch environment variables» قبل
أي بناء — وأعاد commit فارغ البناء فنجح في ١٠ ثوانٍ.)

**قاعدة البيانات** (مُطبَّقة على الحيّة — إضافية فقط، مُوثَّقة في `sql/PHASE0_FOUNDATION.sql`):
- `school_grades` / `school_subjects` لكل مدرسة. المدرسة الحالية زُرعت بالرموز
  `'1','2','3'` حرفياً + ١٢ مادة افتراضية + مادة `test` القديمة (رُبطت).
- الرمز يُولَّد تلقائياً، لا يتغيّر، ولا تُحذف مرحلة فيها طلاب/فصول (مُطلِق).
- `class_teachers.subject_id` — المعلّم ↔ مادة ↔ فصل.
- `promote_school_year` و`import_school_students` تتبعان مراحل المدرسة لا
  `'1','2','3'`. الاستيراد يطابق المرحلة **برمزها أو باسمها** («أول متوسط»).
- حدود المدرسة: `max_students` · `storage_limit_mb` · `ai_daily_per_member` —
  فارغ = بلا حد (السلوك الحالي). الطلاب بمُطلِق، والتخزين في سياستَي الرفع،
  والذكاء في `gemini-proxy.js` (يحلّ محلّ حد الـ١٠، والأسبوعي ×٥، وصفر = لا ذكاء).
- لوحة المالك: `owner_list_schools` · `owner_create_school` · `owner_update_school` ·
  `owner_set_school_admin` — كلها `is_app_admin()` + Google.
- **توافق خلفي**: `school_for_request` بلا slug ← المدرسة **الأقدم** (كانت تُرجع
  null عند تعدّد المدارس فتكسر روابط `?apply=` الموزّعة على الموقع الحيّ).
- فحص الصلاحيات بعد الهجرة: صفر دالّة مكشوفة للمجهول خارج القائمة، صفر
  دالّة مُطلِق مكشوفة.

**الواجهة:**
- `js/34-school-settings.js`: بطاقة «إعداد المدرسة» في إدارة المدرسة —
  روابط الانضمام، المراحل (تسمية/ترتيب/إضافة/حذف)، المواد (تفعيل/تعطيل).
- `js/35-owner-schools.js`: «إدارة المدارس» من أدوات المشرف.
- كل قوائم المراحل من الإعدادات (كانت ثابتة في ٧ مواضع). وأسماء المراحل
  صارت يكتبها المدير ← كل موضع عرض عليه `escapeHtml` (فحص حقن حقيقي، يفشل بدونه).
- إسناد المعلّم: قائمة مواد لا نصّ حرّ. **وأُصلح عطل قديم**: إلغاء إسناد
  معلّم كان يحذف كل موادّه في الفصل نفسه.
- نموذج الطلب يعرض **اسم المدرسة** ومراحلها هي، ويرفض رابطاً بـslug خاطئ.
- الرفع: فحص المساحة قبله، وحذف الملف اليتيم إن فشل حفظ سجلّه.
- **التحميل عند الحاجة على الجوّال فقط**: ١٣ ملف مدرسة (~٢٩٠ ك.ب، ربع
  الجافاسكربت) لا تُحمَّل لطالب خُطى على جوّاله. الكمبيوتر والتطبيق المثبَّت:
  نفس الوسوم بنفس الترتيب حرفياً (document.write في موضعها). `?lazy=0` يعطّله للتشخيص.

**الفحوص:** `tools/فحص-المرحلة-صفر.js` ٢٨/٢٨ (ثلاث مرّات متتالية) · الأزرار ١٣/١٣ ·
السنة ٢٨/٢٨ · إرسال الاختبار ٢٤/٢٤ · وبقية الأدوات كلها ناجحة.
⚠️ `فحص-الإشراف.js`: فشل واحد («ما اقترب موعده يُميَّز باللون») **موجود قبل هذه
المرحلة** — تحقّقتُ بتشغيله على النسخة السابقة فأعطى الفشل نفسه. لم يُعالَج.

### ✅ المرحلة ١ — أُنجزت (٢٤ سبتمبر) على رابط المعاينة نفسه، لم تُدمج في `main`

**ثغرة أمنية سُدّت أولاً:** كان أي طالب يستطيع — بسطر في وحدة تحكّم المتصفّح —
إدراج محاولة بدرجة كاملة أو تعديل درجته بعد التصحيح (سياستا INSERT/UPDATE على
`exam_attempts`). سُحبتا؛ والتسليم عبر `submit_exam_attempt` وحدها كما كان.
تحقّقتُ كطالب حقيقي: «permission denied» والتسليم الطبيعي يعمل.

**قاعدة البيانات** (`sql/PHASE1_HOMEWORK_GRADES.sql`، مُطبَّقة، إضافية):
- الواجب = اختبار بـ`kind='homework'` (نفس المحرّك) + `subject_id` + `shuffle` +
  `late_days` (٠ افتراضياً = لا تأخير). الواجب يحتاج موعداً (مُطلِق)، والحلّ
  يُكشف **بعد انتهاء المهلة كاملة** للجميع — ومنهم من فاته (قرار المالك).
- الخلط لكل طالب ثابت (md5 للاختبار+الطالب)، والإجابة تُحفظ بالرقم **الأصلي**.
  ⚠️ الخلط لا يُطبَّق إلا لواجهة تعلن فهمه (`p_client=2`) — الواجهة المنشورة حالياً
  ترسل بالموضع، فلو خُلط لها لصُحّح طلابها خطأً قبل الإطلاق.
- `official_grades`: درجات نور **كما هي** — أعمدة يختارها المدير بعناوينها وقيمها
  نصّاً (`items=[{label,value}]`). لا حساب ولا متوسّط. الهوية بصمة كالغياب.
  رفعٌ ثانٍ للفترة والمادة نفسها يصحّح ولا يكرّر.
- `student_file` يسجّل كل اطّلاع في `student_file_access` (مرة لكل ١٠ دقائق للشخص).
  المعلّم يرى **موادّه فقط** لطلاب فصوله. الجدولان مغلقان أمام القراءة المباشرة.
- `parent_child_report`: مفاتيح مضافة فقط (kind/late، official، access_log).

**الواجهة:** تبويب «الواجبات» (المنشئ نفسه يُنقل بين التبويبين) · تبويب «السجلّ»
(المعلّم: فصل×مادة، الإدارة: أي فصل، الطالب: سجلّه) · «الملف» في قائمة الأعضاء ·
بطاقة «الدرجات الرسمية من نور» في الإدارة · صفحة وليّ الأمر تعرض الواجب/الاختبار
والتأخير والدرجات الرسمية ومن اطّلع. الدالّة الجديدة `netlify/functions/school-grades.js`.

**الفحوص:** كطالب/معلّم المادة/معلّم آخر/الإدارة على القاعدة الحيّة داخل معاملة
مُلغاة (صفر بقايا) · `tools/فحص-الواجبات.js` ٢٥/٢٥ · `tools/فحص-السجل.js` ٢١/٢١ ·
المرحلة ٠ ٢٨/٢٨ · وليّ الأمر ٢٧/٢٧ · بقية الأدوات ناجحة (عدا فشل الإشراف القديم).
`تدقيق-الدمج.js` صار يفهم وسوم `khutaEager` (كان يعدّ ملفات المدرسة «غير مُدرجة»).

**ما لم يُختبر بعد:** رفع كشف نور حقيقي (لم يصلني ملف فعلي — الأعمدة تُختار
يدوياً فلا يعتمد على شكله) · التدفّق الكامل بحساب Google حقيقي على المعاينة.

### ⚙️ دخول Google على المعاينة كان يعيد للموقع الأساسي (٢٤ سبتمبر) — حُلّ
Supabase لا يعيد إلا لرابط في قائمته المسموحة، وإلا فإلى Site URL. أضاف المالك
في Authentication ← URL Configuration ← Redirect URLs:
`https://deploy-preview-25--khutaa.netlify.app/**` (المعاينة ٢٥ بعينها لا نمطاً عاماً).
يُحذف بعد الدمج النهائي.

### 🐞 «جارٍ التحميل…» للأبد والموقع كأنه تجمّد (٢٤ سبتمبر) — أُصلح
وصف المالك: بعض الخيارات تعلق ولا تعمل إلا بإعادة التحميل أو تغيير المتصفح.
- السبب: طلبٌ لا يعود أبداً (أشهره سفاري الآيباد/الآيفون بعد رجوع التبويب من
  الخلفية: مقبسٌ ميّت). وكل طلب ينتظر الجلسة، فيعلق ما خلفه كلّه.
- ⚠️ ظننتُ أولاً أنه قفل مصادقة Supabase (مستمع onAuthStateChange يستدعي
  القاعدة داخله). جرّبتُه بالمكتبة نفسها (2.112.3) فلم يتكرّر: القفل معطّل
  افتراضياً في هذه النسخة، والإشعارات أثناء البدء مؤجّلة. أبقيتُ تأجيل
  المستمع (توصية Supabase الصريحة، بلا ضرر) لكنه ليس الإصلاح الأساسي.
- الإصلاح: `khutaFetchWithTimeout` في `js/01-core-config.js` — القراءة ١٠ ث
  للمحاولة (والمكتبة تعيد القراءة ٣ مرات بنفسها)، الكتابة ٢٠ ث بلا إعادة
  (قد تكون وصلت)، والرفع بلا مهلة. ثم رسالة «الاتصال بطيء… أعد المحاولة».
  مُختبَر بالمكتبة الحقيقية مع خادم يعلّق: القراءة تنجح بالإعادة، والكتابة
  تُظهر خطأً خلال المهلة.

### ✅ المرحلة ٢ — المكتبة (٢٤ سبتمبر) على رابط المعاينة، لم تُدمج
- `sql/PHASE2_LIBRARY.sql` (مُطبَّقة، إضافية): `school_books` و`school_lessons`.
  الكتاب ملك المدرسة (لا مالك؛ يعدّله من أُسندت له المادة + الإدارة، بإثبات Google).
  ⚖️ **قيد في القاعدة**: رابط الكتاب من `ien.edu.sa` أو `moe.gov.sa` فقط —
  لا رفع للكتاب ولا ربط بموقع آخر (رُفض رابط شبيه `ien.edu.sa.evil.com`).
  الدرس: صفحات + فيديو (https فقط، `javascript:` مرفوض) + اختبار + ملف من المدرسة نفسها.
- `js/37-library.js` تبويب «المكتبة». الطالب يبدأ بمرحلته، ويرى «اختبار الدرس»
  فقط إن أُرسل إليه فعلاً.
- الفحوص: كل الأدوار على القاعدة الحيّة (معاملة مُلغاة، صفر بقايا) ·
  `tools/فحص-المكتبة.js` ١٦/١٦ — وكشف ثلاث مشكلات حقيقية أُصلحت: ترتيب الدروس
  كان يعتمد على الخادم وحده، وصلاحيات تعديل مستخدمٍ سابق تبقى في الذاكرة
  (ظهر المحرّر للطالب بعد تبديل الدور)، وفلاتره كذلك.
- فحص Supabase الأمني: دالّتا الزناد كانتا قابلتين للنداء — سُحبت الصلاحية.

### ✅ المرحلة ٣ — مساحة القدرات (٢٤ سبتمبر) على رابط المعاينة، لم تُدمج
- `sql/PHASE3_GAT_SPACE.sql` (مُطبَّقة): نوع ثالث `kind='practice'` على محرّك الاختبار
  نفسه + `gat_section` (لفظي/كمّي/مختلط). الكشف فوري بعد كل محاولة (مفروض بالزناد).
- **«بلا درجات» مفروض في القاعدة**: سياستا قراءة المحاولات للمعلّم والإدارة تستثنيان
  التدريب — فلا طريق لقراءة اسم أو درجة. العدد وحده من `practice_takers`.
- إعادة المحاولة بلا حدّ للتدريب (٣٠ في الساعة ضد الإغراق الآلي)؛ الاختبار والواجب
  مرة واحدة كما كانا (مُختبَر: ALREADY_SUBMITTED). الطالب يرى محاولاته (`attempts`).
- التدريب **خارج** سجلّ المادة وملف الطالب وتقرير وليّ الأمر.
- الملفات والروابط: عمود `space` في `teacher_files`/`teacher_links` (نفس صلاحيات
  الطالب)، وحدّ الروابط لكل مساحة: ٩ عامة كما كانت، ٤٠ لفيديو القدرات.
  ⚠️ حتى الدمج: الواجهة المنشورة لا تعرف `space`، فأي ملف/رابط قدرات يُضاف من المعاينة
  يظهر أيضاً في قوائمها العامة. يزول بالدمج.
- الواجهة: `js/38-gat-space.js` تبويب «مساحة القدرات». المنشئ نفسه بنوع «تدريب»،
  والمعلّم يرى «اختبره N طالباً» بلا زرّ «النتائج».
- **إصلاح قديم كشفه هذا العمل**: زرّ «راجع إجاباتك» لم يكن يظهر بعد أول محاولة في
  اختبار «فور تسليمه» — `can_review` كان يُقرأ عند الفتح قبل وجود المحاولة. صار يُسأل
  الخادم (`may_review_exam`) بعد التسليم.
- الفحوص: كل الأدوار على القاعدة الحيّة (صفر بقايا) · `tools/فحص-مساحة-القدرات.js` ٢٥/٢٥ ·
  وكل الأدوات الأخرى ناجحة (١٦ أداة).

### ✅ المرحلة ٤ — «ملفاتي» (٢٤ سبتمبر) على رابط المعاينة، لم تُدمج
- `sql/PHASE4_MY_FILES.sql` (مُطبَّقة): مخزن خاص `my-files` (PDF وصور، ١٠ ميجا للملف).
  **الـ٣٠ ميجا في سياسة الإدراج نفسها**: المستعمل + حجم الجديد ≤ 31457280 بايت، و٢٠٠ ملف.
  مُختبَر بسياسة المخزن الحقيقية: ٢٠+١٠ = ٣٠ بالضبط يُقبل، وبايت واحد زيادة يُرفض.
  ⚠️ يعتمد على أن مخزن Supabase يعيد فحص الإدراج عند اكتمال الرفع ومعه الحجم (هكذا
  يعمل إصداره الحالي). لم أستطع تجربة رفع حقيقي من بيئتي (supabase.co محجوب عنها) —
  **يحتاج تجربة من المالك: ارفع ملفات حتى تتجاوز ٣٠ ميجا ويجب أن يُرفض**.
- `is_google_account()`: من `app_metadata.providers` في رمز الدخول — لا مجهول ولا حساب
  «اسم مستخدم وكلمة مرور» (مُختبَر: كلاهما يُرفض).
- لا يقرأ الملف غير صاحبه (مُختبَر بحساب Google آخر: صفر). لا سياسة تحديث (لا استبدال).
  الفتح برابط موقَّع ٦٠ ثانية، ولا زرّ مشاركة في الواجهة.
- «ملفات من خُطى»: مخزن `khuta-files` + جدول `khuta_files` — يرفع ويحذف `is_app_admin()`
  بإثبات Google، ويقرأ أصحاب حسابات Google. (حذف السجلّ بعد الملف لا قبله — وإلا يتيم.)
- الواجهة: `js/39-my-files.js` تبويب «ملفاتي» في خُطى والمدرسة، يُحمَّل عند الفتح على الجوّال.
- الفحوص: `tools/فحص-ملفاتي.js` ١٩/١٩ · كل الأدوات ١٧/١٧ ناجحة.

### ✅ المرحلة ٥ — لوحة المرشد الطلابي (٢٤ سبتمبر) على رابط المعاينة، لم تُدمج
- `sql/PHASE5_COUNSELOR.sql` (مُطبَّقة): `school_members.is_counselor` (طاقم فقط — قيد يمنعها
  عن الطالب)، يضعها المدير من قائمة الأعضاء («اجعله مرشداً»). لا قيمة دور جديدة — كانت ستكسر
  الواجهة المنشورة التي لا تعرفها.
- `counselor_overview`: أرقام خام لكل (طالب، مادة): المُسنَد، المُسلَّم، الفائت (بعد المهلة)،
  ومتوسط المُسلَّم. **من واجبات المنصة واختباراتها فقط** — درجات نور لا يُحسب منها شيء
  (قرار المرحلة ١)، والتدريب خارجها. مُختبَر حيّاً: ٢ مُسنَد (التدريب مستبعد)، ١ مُسلَّم ٢٥٪، ١ فائت.
- الصلاحية: المدير والمرشد فقط (معلّم عادي وطالب: NOT_ALLOWED، والمعلّم لا يجعل نفسه مرشداً).
  المرشد يفتح ملف الطالب كاملاً ويُسجَّل اطّلاعه بصفة «مرشد» (يراها المدير ووليّ الأمر).
- الواجهة `js/40-counselor.js`: جدول الفصل × المادة (متوسط موزون بعدد التسليمات، ونسبة التسليم،
  وألوان: أقل من ٥٠ ضعف، أقل من ٦٥ متابعة)، و«بيانات قليلة» تحت ٥ تسليمات، والمواد الأضعف،
  والطلاب للمتابعة (ضعف في مادة بتسليمين فأكثر، أو فائتان) مع غيابهم من نور. الحدود ثوابت في أعلى الملف.
- **سباق أُصلح** كشفه فحص تبديل الأدوار: فحص صلاحية أقدم يعود متأخراً كان يُظهر التبويب لطالب.
- الفحوص: `tools/فحص-لوحة-المرشد.js` ١٨/١٨ · كل الأدوات ١٨/١٨ ناجحة.
- ملاحظة: في سجلّ الاطّلاع الحيّ صفّ حقيقي واحد — المالك فتح ملف طالب أثناء فحصه (٧:٤٥).

**بهذا اكتملت المراحل ٠–٥ كلها على رابط المعاينة. لا شيء في `main` — الدمج بإذن المالك.**

### 🛠️ ملاحظات المالك بعد فحص المراحل ٠–٥ (٢٤ سبتمبر) — كلها على المعاينة
1. **صور الاختبارات والواجبات والشروح لا تظهر (IMAGE_LOAD_FAILED)** — أهمّها. السبب في
   `netlify.toml`: `img-src 'self' data: blob:` لا يسمح بنطاق المخزن، فالرابط الموقَّع يُنشأ
   بنجاح ثم يرفض المتصفّح الصورة. أُضيف `https://squhkiwjwwyrgufkaujf.supabase.co`.
   ⚠️ **العطل نفسه في الموقع الأساسي الآن** (نفس الترويسة) — يُصلَح بالدمج.
   `tools/فحص-صور-الاختبار.js`: بالترويسة الحالية تُعرض، وبالقديمة تُرفض (ضابط).
2. **الأقسام الجديدة لا تظهر على الهاتف**: الشريط الجانبي مخفيّ تحت ٩٩٢ بكسل وشريط الجوّال
   فيه أربعة ثابتة. أُضيف زرّ «الأقسام» يبني قائمته من الشريط الجانبي نفسه بما يراه المستخدم —
   فأي قسم قادم يظهر تلقائياً.
3. تمييز «الاختبارات» مع «منصة المدرسة» للطالب — أُزيل (كان في styles.css).
4. المكتبة: الطالب لا يختار مرحلته (تلقائي)، والمواد ما له كتب في مرحلته؛ **أي رابط https**
   (قرار المالك — أُلغي قيد منصة عين)؛ و**كتب PDF** يرفعها المعلّم/الإدارة (حدّ ٢ لكل رافع،
   ٢٥ ميجا، مخزن `school-books` يُحسب من مساحة المدرسة) تُعرض بـpdf.js داخل خُطى مع طبقة
   رسم للحل **لا تُحفظ ولا تُرسل** وبلا زرّ تنزيل.
5. «ملفاتي» للمالك: إدارة «ملفات من خُطى» وحدها، بلا مساحة شخصية.
6. مساحة القدرات: لا مادة ولا صفّ ولا خلط؛ والتدريب المنشور **لكل طلاب المدرسة** (في
   `exam_is_assigned_to_me` نفسها)، وملفات القدرات وروابطها كذلك. زرّ «انشر لكل طلاب المدرسة».
7. الطيّ: بطاقات الإدارة (الإعداد، الفصول، الاستيراد، الغياب، درجات نور، الدخول السريع) مطويّة
   افتراضياً وتُحفظ حالتها؛ والمراحل والمواد وروابط الانضمام أقسام مطويّة داخل الإعداد.
8. **ترقية السنة**: نُقلت إلى بطاقة «إجراءات حساسة» مطويّة آخر الصفحة، بزرّ هادئ. ولا تُنفَّذ
   إلا **برمز من ٦ أرقام يُرسل لبريد المدير** (`netlify/functions/sensitive-code.js` عبر Brevo).
   مفروض في القاعدة: الدالّة القديمة أُعيدت تسميتها وأُغلقت، والجديدة تشترط رمزاً صالحاً
   (١٠ دقائق، مرة واحدة، ٥ محاولات، رمز كل دقيقة و٥ في الساعة). البصمة وحدها تُخزَّن.
   ⚠️ حُدّ خطأ قبل التطبيق: رفع الخطأ عند الرمز الخاطئ كان سيُلغي زيادة عدّاد المحاولات.
   ⚠️ زرّ الترقية في الموقع الأساسي القديم لم يعد يعمل (يناديها بلا رمز) — أأمن، ويعود بالدمج.
- الفحوص: `tools/فحص-ملاحظات-المالك.js` ٣١/٣١ (منها عارض PDF بـpdf.js حقيقي) · كل الأدوات ٢٠/٢٠.

### ✍️ الكتابة بخطّ اليد وصور الأسئلة — وفق دليل المالك (٢٤ سبتمبر) — على المعاينة
المالك: «الصورة في السؤال وشرح المعلّم عملت، لكنها ليست أفضل شيء»، وأرفق دليلاً من مشروعٍ آخر.
- **لوحة الرسم** (`js/18-exam-builder.js`) أُعيدت كتابتها: الخطوط **بيانات نسبية 0..1** لا بكسلات
  (وعرض القلم نسبة من العرض)؛ `paintStrokes` **دالّة واحدة** للشاشة وللتصدير؛ Pointer Events مع
  `setPointerCapture` و`touch-action:none`، وإصبعٌ ثانٍ/راحة اليد تُتجاهل؛ الأحداث المدمجة ومنحنيات
  ناعمة؛ نقرة = نقطة. أدوات: قلم، ممحاة (`destination-out`)، ثلاثة أحبار (داكن/أحمر/أزرق)، سُمك،
  **تراجع** (و«مسح الكل» يُتراجَع عنه)، Ctrl+Z وEsc، وتحذير قبل الإغلاق بلا حفظ.
  **التصدير طبقتان** (شفّافة ثم فوق ورقة بيضاء) فالمحو لا يثقب الورق؛ الحبر والورق ثابتان بلا
  ارتباط بالسمة؛ المقاس من القماش الفعلي × max(2, كثافة الشاشة) بسقف ١٦٠٠ يحفظ النسبة. والخطوط
  تُحفظ في ذاكرة الجلسة فيُعاد فتح الرسم ليُكمَل.
- **عرض الصور** — مكوّن واحد `mountExamImage` للمنشئ ولشاشة الطالب: هيكل تحميل بارتفاع ثابت،
  **إعادة توقيع مرّة واحدة** عند فشل التحميل (اختبار طويل ينتهي فيه الرابط) ثم رسالة لا حلقة،
  علم alive ضدّ الردّ المتأخّر عند التنقّل، `loading="lazy"`، `max-width:100%` و`height:auto`
  وسقفٌ للطول، **خلفية بيضاء** (PNG شفّاف بخطوط سوداء كان يختفي في الوضع الليلي)، وذاكرة روابط
  تنتهي قبل الرابط بخمس دقائق. ولمس صورة السؤال أو الشرح **يكبّرها بملء الشاشة**.
- **تصغير قبل الرفع**: صورة أطول ضلعٍ فيها فوق ١٦٠٠ تُصغَّر بنوعها ونسبتها — فصورة جوّال ٨ ميجا
  كانت تُرفض عند حدّ ٥ ميجا، الآن تُقبل وتصل الطالب أسرع. GIF لا يُمسّ.
- 🔴 **ثغرة أُغلقت** (`sql/PHASE7_EXAM_IMAGES.sql`، طُبّق): سياسة قراءة `exam-images` كانت لأي عضو
  في المدرسة، والمسار وإن كان عشوائياً **يُسرَد** (`list` = SELECT) — فالطالب يسرد مجلّد معلّمه
  ويفتح **صورة شرح الحل قبل التسليم**. الآن `exam_image_readable` تشتقّ الإذن من الدوالّ نفسها:
  المعلّم والمدير كما كان؛ الطالب صورة السؤال/الخيار إن كان الاختبار يصله (منشور، مُسنَد) أو
  يحقّ له مراجعته، وصورة **الشرح بعد المراجعة فقط**. مُختبَر بطالب حقيقي قبل وبعد التسليم ومعلّم
  وغريب (قبل التسليم: يسرد ملفاً واحداً بدل ثلاثة، والشرح مرفوض).
- الفحوص: `tools/فحص-الرسم-والصور.js` **٣٤/٣٤** (يقرأ بكسل مكان المحو من الـPNG: 255,255,255,255) ·
  كل الأدوات ٢١/٢١ · تدقيق الدمج: لا ملاحظات · `sw.js` ← v41.

### 🔍 تدقيق خارجي (ChatGPT Codex، ٢٤ سبتمبر) — ما تحقّقتُ منه على القاعدة الحيّة
المدقّق قرأ ملفات SQL في المستودع لا القاعدة الحيّة (قالها بنفسه)، فبعض ملاحظاته عن ملفات قديمة:
1. `import_school_students` مفتوحة؟ **غير صحيح حيّاً**: لا تنفيذ لـanon ولا authenticated (مُختبَر
   بطالب ومجهول: permission denied). لكن الملف لم يكتب السحب صراحةً — أُضيف، وطُبّق صراحةً.
2. الطالب يُدرج درجته؟ **غير صحيح حيّاً**: سُحب INSERT/UPDATE في المرحلة ١ (مُختبَر الآن: permission
   denied). `SCHOOL_SCHEMA.sql` كان ما زال يعرض السياسة القديمة — صُحّح.
3. قصّ صفحة الدخول على الجوّال: **لم يتكرّر** — قياس 360/390/430 (جوّال وسطح مكتب ضيّق وشاشة الترحيب):
   لا عنصر يتجاوز حافة الشاشة. يُطلب من المالك لقطة إن رآه.
4. إلغاء الاشتراك بـGET: **صحيح، وأسوأ مما قال**: كان GET يُلغي (ماسحات البريد تُلغي دون علم)، وPOST
   يُرفض — مع أن رسائلنا تعلن `List-Unsubscribe-Post` فكان زرّ Gmail نفسه لا يعمل. أُصلح: GET صفحة
   تأكيد، وPOST (الزرّ أو Gmail) يُلغي.
5. `unsafe-inline` في CSP: صحيح ومعروف (مئات onclick) — مشروع مستقل لاحقاً.
6. حماية كلمات المرور المسرّبة: **ما زالت مطفأة** (فحص Supabase يؤكّد) — إعداد في لوحة Supabase على المالك.
7. المستودع **عام** فعلاً: فحصتُ ٢٠٩ commit — لا مفتاح سرّي في التاريخ. التوصية: جعله خاصاً (Netlify يعمل معه).
8. SRI لمكتبة Supabase: **موجود أصلاً** (`integrity` في index.html) — ملاحظة غير صحيحة.

### الفحص الشامل (٢٤ سبتمبر) — كل الأدوات ناجحة
المرحلة ٠ ٢٨ · الواجبات ٢٥ · السجلّ ٢١ · المكتبة ١٦ · وليّ الأمر ٢٧ · دوال الخادم ٥١ ·
واجهة الاختبار ٣٣ · تقرير الفصل ٣٦ · السنة ٢٨ · مغادرة المدرسة ٢٥ · إرسال الاختبار ٢٤ ·
الأزرار ١٣ · الإشراف **٣١/٣١** (فشله القديم كان تاريخاً ثابتاً في بيانات الفحص
«2026-09-16» مضى فصار الصفّ «عاجلاً» — صار نسبياً؛ لم يكن عطلاً في الموقع) ·
تدقيق الدمج: لا ملاحظات · كل ملفات JS تُحلَّل بلا خطأ.

**ما يحتاج المالك تجربته بنفسه على رابط المعاينة** (يتطلّب حسابه الحقيقي):
إنشاء مدرسة تجريبية من لوحة المالك · فتح رابط انضمامها من جهاز آخر ·
ضبط حدّ طلاب صغير وتجاوزه · إسناد معلّم لمادة.
⚠️ المدرسة التجريبية تُنشأ في قاعدة الإنتاج نفسها (مشتركة) — آمن بفضل
التوافق الخلفي أعلاه، ولا يوجد زرّ حذف مدرسة بعد (يُضاف عند الطلب).

---

## 🔴 ثغرة حقيقية أُصلحت اليوم: إجابات الاختبار كانت تصل الطالب مباشرة

أثناء إضافة "شرح الحل" لكل سؤال، تبيّن أن `teacher_exams.questions`
(الإجابة الصحيحة لكل سؤال) كان يُقرأ **مباشرة من الجدول** بلا أي حجب —
`js/14-school-work.js` (loadTeacherExams) كان يطلب هذا العمود فقط ليحسب
عدد الأسئلة، فيصل معه محتواه كاملاً لأي طالب فتح تبويب الاختبارات، قبل
الضغط على "ابدأ الاختبار" حتى. لا اختراق مطلوب: استخدام عادي للتطبيق.

**السبب:** سياسات RLS تُقيّد الصفوف لا الأعمدة، ومنح Supabase الافتراضي
يغطّي كل الأعمدة. `get_exam_for_student`/`get_exam_review` كانتا تحجبان
الإجابة بعناية — لكن الواجهة كانت تتجاوزهما بقراءة الجدول مباشرة.

**الإصلاح** (مُطبَّق على الإنتاج، مُوثَّق في `sql/EXAM_FUNCTIONS.sql`):
عمود `question_count` مولَّد بدل قراءة `questions`، وسُحبت صلاحية قراءة
عمود `questions` كلياً من `authenticated`/`anon` — لا يُقرأ الآن إلا من
دوال SECURITY DEFINER (تعمل بصلاحية المالك، غير متأثرة).

**تحذير للمستقبل:** أي عمود جديد حسّاس يُضاف لجدول له RLS صفّي فقط
(كـ`explanation` اليوم) يحتاج فحصاً صريحاً: هل تقرأه الواجهة مباشرة من
الجدول في أي مسار؟ إن كان كذلك، RLS الصفّي وحده لا يحميه.

---

## 🟢 نُشر على الإنتاج

`khutaa.netlify.app` يحمل الآن منصّة المدرسة كاملة. PR #24 دُمج ونُشر —
كوميت `30a0779`، ١٤ دالّة خادم، فحص أسرار نظيف. **بقرار صريح من المالك:
نُشر قبل تشغيل فحص صلاحيات الدوال على القاعدة الحيّة** (الملف المُرسَل
`فحص-قبل-النشر.sql`) — لم يُنفَّذ بعد وقت النشر. راجع القسم أدناه.

---

## ١) المشروع

**خُطى (Khuta)** — منصّة سعودية عربية لتحضير اختبار القدرات (GAT)، على
`khutaa.netlify.app`. بناها المالك لنفسه ولطلاب القدرات.

ثم انضمّت إليها **منصّة مدرسية** لـ**مدارس المتقدمة — فرع الملقا**.

### القرار المعماري المُلزِم

**منصّة واحدة لا اثنتان.** `khutaa.netlify.app` يبقى كما هو لأي مستخدم
عادي. وأقسام المدرسة تظهر **فقط** لمن أضافته إدارة المدرسة ببريده في
Google — لا بالعنوان، ولا برابط سرّي، ولا بكلمة مرور مشتركة.

سبب رفض نسخة ثانية من المشروع: نسخة ثانية من ١٦ ألف سطر تعني إصلاح كل
خلل مرّتين، وخلال أسابيع تفترق النسختان فيصير كل تعديل مخاطرة.

---

## ٢) قيود ثابتة من المالك — لا تُخالَف

| القيد | نصّه |
|---|---|
| بيانات نور | «لن يتم إضافة أي بيانات رسمية من نور أو أي بيانات شخصية للطالب» — **عُدِّل ٢٣ سبتمبر**: الغياب والدرجات الفصلية تُعرض من ملف يصدّره المدير من نور (عرض لا ربط، بلا رقم هوية مخزَّن) |
| رقم الهوية | لا يُحفظ. تُحفظ منه بصمة HMAC بفلفل سرّي للمطابقة فقط |
| الألقاب | **النظام يولّدها فقط.** الطالب لا يكتب لقباً أبداً — «لتجنب مشكلات كتابة اسم غير لائق». له أن يضغط «غيّر» فيولّد النظام غيره |
| الطالب المنقول | «سيتم حذف حسابه فقط من جهة المنصة الخاصة بالمدرسة لكن حسابه سيبقى على منصة خطى» |
| بيانات السنة الماضية | تُخفى عن الطلاب فور بدء السنة الجديدة، وتُحذف بعد أسبوع |
| البلاغات | **خمسة** بلاغات → إخفاء (لا حذف) → صندوق الإدارة → حذف تلقائي بعد أسبوع |
| الحضور | **عرض** لملف يُصدّره المدير من نور — لا تحضير يدوي، ولا ربط مع نور |

### تعليمات تشغيلية

- **البريد:** طُلب سابقاً إرسال بريد لـ`soosrakan1430@gmail.com` و
  `RAKAN8022@icloud.com` بعد كل ردّ. **مؤجَّل بقرار المالك**: «بخصوص البريد
  نرى له حلاً لاحقاً، الآن نريد إنجاز الموقع بشكل مثالي».
- **الفرع:** كل التطوير على `claude/system-security-audit-xijbr6`.

---

## ٣) الحالة الآن (١٢ سبتمبر)

### الروابط

| الرابط | الحالة |
|---|---|
| `khutaa.netlify.app` | ⚠️ نسخة **١٦ أغسطس** — موقع القدرات وحده، بلا منصّة مدرسة |
| `deploy-preview-23--khutaa.netlify.app` | ✅ **يعمل ممتازاً** — المالك أكّده. فيه كل الميزات |
| `deploy-preview-24--khutaa.netlify.app` | ✅ يعمل أيضاً بعد التحديث والانتظار. يدخل الإدارة والمعلّم والطالب بلا مشاكل ودون تحويل للرابط الأساسي |

**المالك يميل للانتقال إلى ٢٤.**

### لماذا الموقع الأساسي قديم؟

**رصيد البناء في Netlify نفد** — ليس قفلاً للحساب ولا عقوبة. دورة الفوترة
١٧ أغسطس → ١٦ سبتمبر، والنشر الإنتاجي مرفوض `403`. **الرصيد يعود ١٧
سبتمبر.** ومعاينات طلبات الدمج تُبنى بنجاح رغم نفاد الرصيد — مُتحقَّق.

**والحساب نفسه مفتوح وسليم**: `sandbox:false`، خطّة Free، المالك هو الـOwner،
موقعان (`khutaa` و `motaqadima-khuta`).

### قاعدة البيانات (Supabase — مشروع `squhkiwjwwyrgufkaujf`)

مشروع **واحد** يضمّ خُطى وجداول المدرسة معاً. **لا يوجد مشروع اسمه «خُطى
سكول»**. (مشروع `gwbhwshxvlagmoonhfha` فارغ ويجب حذفه.)

| | |
|---|---|
| المدرسة | `motaqadima` / مدارس المتقدمة — فرع الملقا |
| الأعضاء | admin: RAKAN ALHARBI `soosrakan1430@gmail.com` · teacher: `ana btata` · student: `hrb mhcg` |
| الفصول | `3/e` ✅ فيه المدرّس والطالب · `1/A` ❌ فارغ · `2/C` ❌ فارغ |
| محاولات الاختبار | ٠ · لقطات الحضور: ٠ |

⚠️ **للعرض استعمل `3/e` وحده.** الفصلان الآخران فارغان تماماً.

### الاجتماع

**الأحد ١٣ سبتمبر** مع إدارة المدرسة. أي: الرصيد يعود **بعده بأربعة أيام**.

---

## ٤) قرارات المالك المحسومة — لا تُعَد إثارتها

| القرار | نصّه |
|---|---|
| ❌ فصل تجريبي / بيانات مزروعة | «أمام الإدارة لن أتمكّن من الانتقال لحساب الفصل التجريبي أو الطالب التجريبي لأريه أنه حقاً انتقل أو حقاً وصله الملف أو الرابط» |
| ❌ تغيير أسماء حسابات التجربة | «هذي فقط أسماء حسابات لديّ للتجربة، وبالتأكيد المدير سيدخل بحسابه هو» |
| ✅ عرض حيّ بجهازين | الحاسوب معلّم، الجوّال طالب — يُثبت الوصول فعلاً |
| ✅ النشر على khutaa مباشرة | اختاره حين سُئل، فدُمج الفرع في `main` |

---

## ٥) سجل ما أُنجز

### الخطّة المتّفق عليها — عشرة بنود، كلّها مكتملة

1. ✅ استيراد الطلاب دفعةً واحدة ببصمة هوية مشفّرة
2. ✅ قلب المعمار إلى «دور + وضع»
3. ✅ ألقاب مستعارة يولّدها النظام لكل الطلاب
4. ✅ محرّك اختبارات موحّد
5. ✅ تقرير الفصل القابل للطباعة
6. ✅ الإشراف على المجتمع (٥ بلاغات → إخفاء → صندوق الإدارة → حذف بعد أسبوع)
7. ✅ وصول وليّ الأمر برابط محسوب HMAC + تصدير Excel + عدّاد إلغاء
8. ✅ الترقية الجماعية آخر السنة + سياسة حذف البيانات
9. ✅ مغادرة المعلّم/الطالب للمدرسة
10. ✅ الحضور كعرضٍ لملف نور

### الملفات المُنشأة

**واجهة:** `js/28-report.js` · `js/29-moderation.js` · `js/30-parent-links.js`
· `js/31-year.js` · `js/32-member-leave.js` · `js/33-attendance.js` ·
`parent.html`

**خادم:** `netlify/functions/parent-links.js` · `parent-view.js` ·
`school-attendance.js` · `moderation-sweep.js` · `school-year-purge.js` ·
`notify-owner.js`

**فحوص:** `tools/فحص-واجهة-الاختبار.js` (33) · `فحص-إرسال-الاختبار.js` (24)
· `فحص-تقرير-الفصل.js` (36) · `فحص-الإشراف.js` (31) ·
`فحص-صفحة-ولي-الأمر.js` (27) · `فحص-السنة-الدراسية.js` (28) ·
`فحص-مغادرة-المدرسة.js` (25) · `فحص-دوال-الخادم.js` (51) ·
`فحص-الأزرار-والبصر.js` (**13 ناجح / 0 فاشل**) · `فحص-المربع-الأبيض.js` ·
`فحص-صلاحيات-الدوال.sql` · `تدقيق-الدمج.js` (لا ملاحظات)

**وثائق:** `سيناريو-العرض.md` · `خطوات-النشر.md` · `سجل-المشروع.md` (هذا)

**وثيقة للمدرسة (Artifact):**
`https://claude.ai/code/artifact/4d51bc5b-c21f-437a-abd9-0723beeec9c0`
عنوانها «منصّة خُطى للمدارس». طلب المالك أن تُوضّح أنّ الموقع **كان في
الأصل مشروعاً شخصياً لطلاب القدرات** ثم دُمجت منصّة المدرسة فيه — «٢ في ١».
وأن تكون بلغة بسيطة غير رسمية جداً، ولا يتّضح أنّها من ذكاء اصطناعي.

---

## ٦) أخطاء حقيقية وُجدت وأُصلحت في المنتج

1. **فشل إرسال صامت** — إدراج PostgreSQL ذرّي: الإرسال لفصل مُسنَد مسبقاً
   + فصل جديد يرفع 23505 فيتراجع **كل** شيء. المعلّم يرى «أُرسل ✅» والفصل
   الجديد لم يصله شيء.
2. **الإدارة لا تقرأ أي محاولة اختبار** — لا سياسة SELECT للإدارة على
   `exam_attempts`. قياس: admin=0 صف، teacher=1.
3. **تواريخ هجرية** — `"ar-SA"` وحده يعطي الهجري. الصحيح
   `"ar-SA-u-ca-gregory-nu-latn"`.
4. **الطالب المُرقّى يبقى في فصلين** — الترقية تضيف العضوية ولا تزيل القديمة.
5. **تسليم في سنة مؤرشفة** — تبويب قديم مفتوح ليلة الترقية.
6. **ثغرة أدخلتُها أنا** — `latest_attendance()` بـSECURITY DEFINER
   وEXECUTE عامّ: **زائر مجهول** يقرأ غياب أي طالب عبر `/rest/v1/rpc/`.
   ⚠️ `EXECUTE` ممنوح لـ`PUBLIC` افتراضياً في Postgres.
7. **قاعدة انتحال واسعة** — رفضت «شكرا لك يا استاذ على الشرح».
8. **XSS كِدتُ أُدخله** — تمرير اسم فصل في `onclick`. المتصفّح يفكّ ترميز
   السمة قبل قراءتها. الحلّ: تمرير المعرّف فقط.
9. **تباين الزرّ الأساسي** — أبيض على ذهبي `#C9962E` = 2.66 مقابل حدّ 4.5.
   أُضيف `--gold-btn: #976F20` = 4.55. و`--gold` لم يتغيّر.
10. **🔲 المربّع الأبيض** — انظر القسم التالي.

---

## ٧) المربّع الأبيض — القصّة كاملةً

**⚠️ المالك وصفه مرّتين. في الثانية قال إنّ ذاكرتي ضعيفة، وكان محقاً:
سألتُه من الصفر بدل الرجوع لوصفه.**

### وصف المالك

- «له فترة طويلة نحاول حلّه ولم يُحلّ»
- «حتى إنه لا يتضح في الصورة، لا أعرف لماذا»
- **«كان في مكان خانة التحويل بين معلم/مدير/طالب»** ← المفتاح
- **«الموقع الوحيد الذي لا يحوي المربّع الأبيض هو `khutaa.netlify.app`»** ← المفتاح الثاني
- «يرفع ضغطي»

### السبب — بالقياس لا بالتخمين

`.demo-bar` (شريط التحويل بين الأدوار) خلفيته `var(--card)`.

**ومتغيّر بهذا الاسم غير معرَّف في المشروع إطلاقاً.** الموجود
`--card-light` و`--card-dark` و`--surface` الذي يختار بينهما.

و`var()` غير المحلول **يُبطل الخاصية كلّها** فتعود لقيمتها الابتدائية:
`background-color: transparent`.

فكان الشريط **مستطيلاً مثبّتاً بعرض الشاشة، ارتفاعه 62px، فوق كل شيء
(`z-index:9998`)، بلا خلفية** — يُظهر ما خلفه.

**ولماذا لا يظهر على `khutaa.netlify.app`؟** لأنّ وضع العرض محصور بحكم
الكود في مضيفات Netlify التي تحوي `--`. فالشريط لا يُنشأ هناك أصلاً.

قياس قبل الإصلاح: `barBg: rgba(0, 0, 0, 0)` في الوضعين، و`--card` = `[]`.

### ⚠️ تشخيصان خاطئان قبل الصحيح

**الخطأ الأول: `var(--card)` في `.demo-bar`.** صحيح أنّ المتغيّر غير معرَّف
وأنّ ذلك عيب حقيقي أُصلح (`var(--card)` → `var(--surface)` في `.demo-bar`
و`.tt-slot`)، **لكنّه لم يكن سبب المربّع**. نُشر الإصلاح وبقي المربّع.

> ولا يصحّ إصلاح ذلك العيب بـ`--card: var(--surface)` على `:root` —
> المتغيّر يُحسب حيث يُعلَن، فيُحسب على قيمة الوضع الفاتح ويُورَّث أبيض.

**الخطأ الثاني: صورة مكسورة يحجبها Brave.** وُسّعت الكنسة لتلتقط الصور
التي يفشل تحميلها (لا الفارغة فقط) وأُضيفت `MutationObserver` — تحسين
حقيقي باقٍ، **لكنّه لم يكن السبب أيضاً**: جرّب المالك Edge فظهر المربّع
نفسه، فبطل تفسير Brave.

### السبب الحقيقي — شريط تمرير الصفحة

في `styles.css` داخل `@media (min-width:993px)`:

```css
* { scrollbar-width: thin; scrollbar-color: var(--border) var(--bg-alt); }
```

وهي تنطبق على `html` أيضاً. و`html` يقرأ **القيم الفاتحة** لأنّ `.dark-mode`
على `body` لا على `html`. **وشريط تمرير الصفحة يُرسم من الجذر** — فيخرج
بلون الورق `#E8E1D2` فوق صفحة داكنة.

**القياس الحاسم:**

| | scrollbar-color |
|---|---|
| `html` | `rgb(221,212,192) rgb(232,225,210)` ← فاتح |
| `body` | `rgb(41,53,82) rgb(13,20,36)` ← داكن صحيح |

**ولماذا الكمبيوتر وحده؟** لأنّ تنسيق الأشرطة كلّه داخل
`@media (min-width:993px)`، والجوّال والآيباد يستعملان أشرطة عائمة تختفي
وحدها. وهذا يطابق «لا يظهر في الآيباد أو الهاتف أبداً» حرفياً.

**ولماذا لا يظهر في لقطة الشاشة؟** لأنّ أشرطة التمرير ليست جزءاً من
محتوى الصفحة، وأدوات اللقطات في المتصفّح لا تلتقطها — ولذلك صوّره المالك
بكاميرا الهاتف.

### الإصلاح

`applyThemeChrome()` في `js/05-boot-nav.js` تضبط الآن
`documentElement.style.scrollbarColor` مع الخلفية — وهي الدالّة المخصّصة
أصلاً لما لا يصله CSS لأنّ الوضع الداكن على `body`.

⚠️ **ولا يُصلحه `body.dark-mode ::-webkit-scrollbar-track` الموجود في
styles.css**: حين تُضبط `scrollbar-color` القياسية يتجاهل كروم الحديث
أشباه العناصر `::-webkit-scrollbar-*` بالكامل — فتلك القاعدة شفرة ميتة
في المتصفّحات الحديثة (وتبقى مفيدة للقديمة).

قياس بعد الإصلاح: داكن `rgb(13,20,36)` سطوع 0.08 · فاتح `rgb(232,225,210)` سطوع 0.88 ✅

**⚠️ وهذا أيضاً لم يكن السبب.** نُشر الإصلاح، وأثبتت لوحة التشخيص أنّ
شريط تمرير `html` صار داكناً `rgb(13,20,36)` فعلاً — والمربّع باق. فهو
إصلاح صحيح لعيب حقيقي، لا أكثر.

### ✅ السبب الحقيقي — درج Netlify المحجوب بـCSP

لوحة التشخيص (`?diag=1`) على جهاز المالك أعطت:

```
• IFRAME#-.- | rgba(0,0,0,0) | static | w=780 h=48
• DIV#-.-    | rgba(0,0,0,0) | fixed  | w=780 h=48
```

إطار `iframe` بمقاس **780×48** داخل `div` مثبَّت أسفل الوسط. وفي وحدة
تحكّم المالك كان الجواب مكتوباً حرفياً من قبل:

```
Framing 'https://app.netlify.com/' violates the following Content Security
Policy directive: "default-src 'self'". The request has been blocked.
```

**Netlify يحقن في روابط المعاينة وحدها** أداة تعاون (Netlify Drawer) هي
إطار يعرض `app.netlify.com`. وسياسة CSP في `netlify.toml` عندنا
`default-src 'self'` بلا `frame-src` — فيُحجب الإطار، والإطار المحجوب
يرسمه المتصفّح **مستطيلاً أبيض فارغاً بأيقونة مستند مكسور**.

**والموقع لا يُنشئ إلا إطاراً واحداً** (`js/10-account.js`) وهو
`display:none` — فُتحقِّق أنّ الإطار ليس من الموقع.

| علامة وصفها المالك | التفسير |
|---|---|
| **لا يظهر على `khutaa.netlify.app` وحده** | Netlify لا يحقن الدرج في الإنتاج، بل في المعاينات فقط. **أقوى علامة، وأهملتُها** |
| أسفل الوسط، عريض | 780×48 — مقاس الدرج |
| Brave وEdge معاً | العلّة في CSP لا في المتصفّح |
| لا يظهر في الآيباد ولا الجوّال | الدرج لسطح المكتب |
| يظهر بعد الدخول لا في شاشة التحميل | شفرة الدرج تُحقن بعد الرسم |
| أيقونة ورقة مكسورة | شكل الإطار المحجوب في Brave (وEdge يرسمه أبيض فارغاً) |

### الإصلاح

`khutaHideBlockedNetlifyDrawer()` في `js/05-boot-nav.js` تُخفي الحاوية
المثبَّتة لأي إطار مصدره على `app.netlify.com`، وتُنادى من الكنسة فتشملها
المراقبة. مُتحقَّق: درج محاكى 780×48 أُخفي تلقائياً، وإطار غير تابع
لـnetlify لم يُلمس.

⚠️ **ولم نُوسّع CSP لتسمح بـ`app.netlify.com`**: لو سُمح له لظهر شريط
تعاون من Netlify أمام إدارة المدرسة — أسوأ من مربّع أبيض.

➡️ **والعلاج الجذري بيد المالك**: Netlify → إعدادات الموقع →
Deploy Previews → إيقاف **Netlify Drawer**. والدالّة شبكة أمان لا بديل.

### درس — ثلاثة تشخيصات خاطئة قبل الصحيح

المالك وصف علامةً واحدة كانت كافية من أوّل مرّة: **«الموقع الوحيد الذي
لا يحوي المربّع هو `khutaa.netlify.app`»**. وما يفرّق الإنتاج عن المعاينة
ليس شفرة الموقع — بل ما يحقنه Netlify. وأنا طاردتُ متغيّر CSS، ثم صورة
مكسورة، ثم شريط تمرير.

وأسوأ من ذلك: **رسالة CSP كانت في أوّل صورة أرسلها، وصرفتُها بأنّها «من
تبويب Netlify لا من موقعك»** — وكانت هي الجواب بحروفه.

**القاعدة المستخلصة:** حين يقول المالك «يحدث هنا ولا يحدث هناك»، فابدأ
بما يفرّق الموضعين قبل أي شيء آخر. ولا تصرف رسالة خطأ في وحدة التحكّم
دون التحقّق من مصدرها.

---

## ٧ب) خطأ ٤٠٤ منفصل — بنك الأسئلة

وحدة التحكّم تُظهر:

```
Failed to load resource: 404 — raw.githubusercontent…%D9%84%D8%A9.json
```

الرابط ينتهي بـ`لة.json` أي **`بنك الأسئلة.json`** — غير موجود في مستودع
`rakan1430/my-website-data`. (يُجلب في `js/11-community.js:688`.)

لا يكسر الموقع — الكود يرجع للبيانات المدمجة — لكنّه يعني أنّ بنك الأسئلة
البعيد لا يُحدَّث. يُصلَح برفع الملف للمستودع أو حذف الجلب.

---

## ٨) أخطاء ارتكبتُها أنا — لئلّا تتكرّر

### في الفحوص

- قِست التوسيط مقابل `window.innerWidth` والقائمة الجانبية تأخذ 300px
- توقّعت الإجابة `{"1":1}` والصحيح `{"0":1}` (المفتاح رقم السؤال)
- استعملت `getComputedStyle` لخلفية الطباعة بدل عيّنة بكسل مطبوع
- أخذت العيّنة على حرف بدل ورق فارغ
- قِست الألوان قبل انتهاء انتقال نصف الثانية على `body`
- انتقلت لنفس العنوان+الشذرة متوقّعاً إعادة تحميل
- `waitForSelector` الافتراضي `visible` على عنصر داخل تبويب مخفي
- Regex التقط أسماء توصيفات بعد النقطة (`getElementById`)
- قرأت `rgba(0,0,0,.05)` كخلفية سوداء فأعطى تبايناً مستحيلاً 1.19
- **وفي هذه الجلسة:** أضفت `dark-mode` يدوياً فأنشأت حالة لا تحدث لمستخدم
  حقيقي (body داكن وhtml فاتح لأنّ `applyThemeChrome` لم تُستدعَ) وكِدتُ
  أبلّغ عن مربّع أبيض من صنع الفحص نفسه
- **وفيها أيضاً:** نمط `js/[0-9][0-9]-[a-z-]*\.js` لا يطابق `03-i18n.js`
  لأنّ `i18n` فيها أرقام — فاتّهمتُ ملفاً سليماً بأنّه غير مُحمَّل

### في القرارات

- **دمجتُ PR #23 وأغلقتُه** وكان وصفه يقول بالخطّ العريض «أبقِ هذا الطلب
  مفتوحاً — رابط معاينته هو رابط العرض». لم أقرأ التحذير قبل الدمج، وكان
  مكتوباً في المستودع. (تبيّن لاحقاً أنّ معاينة ٢٣ بقيت حيّة فلم يقع ضرر.)
- **أعدتُ سؤال المالك عن المربّع الأبيض** وقد وصفه سابقاً — وهو سبب هذا الملف.
- أفرطتُ في «الخبطة» حول الروابط حتى قال المالك: «لا داعي لكل هذه الخبطة».

### دروس تقنية تتكرّر

- `const`/`let` في سكربت كلاسيكي متغيّرات معجمية **ليست** في `window` —
  لكن `onclick` يراها. الفحص بـ`window[name]` وحده يتّهم أزراراً سليمة.
- إدراج PostgreSQL **ذرّي**: 23505 واحد يُلغي كل الصفوف.
- `raise exception` في PL/pgSQL يتراجع بكل شيء في المعاملة.
- `EXECUTE` ممنوح لـ`PUBLIC` افتراضياً، وSupabase يكشف كل دالّة عامّة على
  `/rest/v1/rpc/`. سياسات RLS وحدها لا تكفي مع دوالّ SECURITY DEFINER.
- الشذرة `#…` لا تصل أي خادم — تُستعمل لحمل رموز أولياء الأمور.
- CSV يحتاج **BOM** ليقرأه Excel العربي على ويندوز.
- `--card` غير معرَّف. تحقّق من وجود المتغيّر قبل استعماله.

---

## ٩) معلّق — ينتظر المالك

| البند | الحالة |
|---|---|
| `OWNER_NOTIFY_SECRET` في Netlify (٢٤ حرفاً فأكثر) | لإشعار المالك وحده — لا يلزم العرض |
| حماية كلمات المرور المسرّبة في Supabase → Auth | لم تُفعَّل |
| تحويل مستودع GitHub إلى `private` | عامّ الآن. لا مفاتيح فيه (فحص الأسرار: ٠ تطابق) |
| اسم الفصل `3/e` | سيظهر للحضور |
| إشعارات الجوّال | سُئل ثلاث مرّات ولم يُجَب |

### لا يُوعَد به في الاجتماع

- **البريد التلقائي** — الدوال المجدولة لا تعمل إلا إنتاجياً (بعد ١٧ سبتمبر)
- **تطبيق جوّال** — لا يوجد؛ الموقع يُضاف للشاشة الرئيسية
- **أي ربط مع نور** — لا يوجد ولن يوجد

---

## ١٠) أشياء لم تُختبر قطّ

- رفع صورة سؤال على الحاوية الحقيقية
- الموقع على سبورة أندرويد حقيقية
- الدوال المجدولة (لم تعمل قطّ — تحتاج نشراً إنتاجياً)
