# الفحص الأمني — خُطى ومنصة المدرسة

آخر تحديث: ٩ سبتمبر ٢٠٢٦

هذا سجلٌّ لما فُحص فعلاً، وكيف فُحص، وما وُجد. القاعدة التي حكمت العمل:
**لا يُقبل بندٌ لأنه يبدو سليماً في الكود — يُقبل لأن هجوماً حقيقياً جُرِّب عليه وفشل.**
وقد أثبت هذا الفحص جدواه: ثلاث ثغرات من الخمس أدناه بدت سليمة تماماً بالقراءة،
ولم تظهر إلا عند التشغيل.

---

## ١) ما وُجد وأُصلح

### أ. تسريب بريد كل مستخدم في المنصة — **خطير**

`resolve_username_email(username)` كانت تُرجع البريد الحقيقي لمن يعرف اسم المستخدم
وحده. وأسماء المستخدمين **معروضة علناً** في لوحة المتصدّرين والمجتمع. أي أن من
يقرأ اللوحة يحصد الأسماء، ثم يحوّل كل اسم إلى بريد حقيقي — قائمةُ بريدٍ كاملة
لمنصة فيها آلاف الطلاب، جاهزة للتصيّد.

**الإصلاح:** الدالة صارت تطلب كلمة المرور وتقارنها بالتعمية (`crypt`) داخل
قاعدة البيانات. من يعرف كلمة المرور لا يكسب شيئاً جديداً، ومن لا يعرفها لا
يحصد شيئاً. والردّ لا يفرّق بين "اسم غير موجود" و"كلمة مرور خاطئة" — كلاهما
`null`.

**والأثر الجانبي الذي وجب حلّه:** مسار "نسيت كلمة المرور" بطبيعته لا يملك
كلمة المرور. فنُقل التحويل كلّه إلى الخادم: `netlify/functions/password-reset.js`
يحوّل الاسم بمفتاح الخدمة عبر `resolve_username_email_admin` (لا يملك تنفيذها
إلا `service_role`)، ويطلب من Supabase إرسال الرسالة. **البريد لا يصل المتصفح
في أي فرع من فروع التنفيذ**، والردّ واحد لا يتغيّر أبداً فلا يُستدلّ منه على
وجود حساب.

### ب. القفل الذي لم يكن يقفل — **خطير**

رقم مطابقة السبورة كان **٤ خانات** (١٠٬٠٠٠ احتمال) بنافذة ٩٠ ثانية و**بلا أي
حدّ للمحاولات**. من يملك `public_id` يجرّب كل الاحتمالات عبر REST خلال النافذة.
والنتيجة ليست سرقة حساب المعلّم بل العكس: تُفتح على السبورة جلسةُ المهاجم،
فيكتب المعلّم في حساب غيره وهو يظنّه حسابه.

وحين أضفتُ عدّاد المحاولات، **لم يعمل** — وأثبت الاختبار أن ٨ محاولات خاطئة
تمرّ كلها. السبب أن `raise exception` في PL/pgSQL يُجهض المعاملة، فيُلغى معها
التحديثُ الذي سبقه في نفسها. العدّاد كان يُكتب ثم يُمحى في نفس النَّفَس.

**الإصلاح:** ٦ خانات من `gen_random_bytes` (مولّد تعموي آمن، لا `random()` التي
ليست كذلك)، وقفلٌ بعد ٥ محاولات يُبطل الطلب نهائياً، والمخالفةُ صارت **قيمةً
راجعة لا استثناءً** كي يثبت العدّاد.

### ﺝ. بريد نتيجة الاختبار لم يصل طالباً واحداً منذ كُتب

`send-email.js` كان يستعمل متغيّراً باسم `username` **لا وجود له في الملف**.
الصياغة سليمة (`node --check` يمرّ)، والمراجعة بالعين مرّت عليه مراراً، ولم
يظهر إلا عند التشغيل: `ReferenceError` يبتلعه `catch` فيعود 500 صامتاً.

**الإصلاح:** الاسم يُجلب من حساب المستخدم المُتحقَّق منه على الخادم. ولئلّا
يتكرّر هذا الصنف من الأعطال، أُضيف `tools/فحص-دوال-الخادم.js` الذي **يشغّل**
دوال Netlify فعلاً بشبكة مُقلَّدة ويسقط عند أي `ReferenceError` أو 500.

### د. باب الطلبات العلني بلا حدّ

`account_requests` مفتوح للإدراج لغير المسجّلين — وهذا لازم، فهو مسار تقديم
المعلّم. لكنه كان **بلا أي سقف**: أي زائر يغرق قائمة الإدارة بآلاف الطلبات
المزيّفة، فتصبح غير قابلة للمراجعة يوم العرض على المدرسة.

**الإصلاح:** حارس في قاعدة البيانات نفسها (لا في المتصفح — الحارس الذي يعيش في
المتصفح ليس حارساً): بريد صالح، اسم حقيقي، لا طلب معلّق مكرّر بنفس البريد،
وسقف ٣٠ طلباً في الساعة لكل مدرسة. وكل رفضٍ له رسالة تقول سببه وما يفعله
المستخدم — الرسالة السابقة كانت "حاول مرة أخرى"، وهي أسوأ نصيحة ممكنة لمن
رُفض طلبه لأنه مُرسَل مسبقاً.

### هـ. توليد رمز غير آمن + إغراق جدول

`screen_login_start` مفتوحة لغير المسجّلين (لازم — السبورة ليست مسجّلة بعد)
وكانت بلا سقف: حقن ملايين الصفوف ممكن. أُضيف سقف ٦٠ إنشاءً في الدقيقة.

---

## ٢) ما جُرِّب عليه الهجوم وصمد

### الضيف المجهول

خُطى تنشئ لكل ضيف **جلسة Supabase مجهولة صامتة** (يحتاجها المجتمع). ودورها
`authenticated` تماماً كأي طالب مسجَّل — فأي سياسة كُتبت لـ`authenticated`
تشمله. هذا أخطر ما في التصميم، لأنه غير بديهي.

جُرِّب فعلاً بجلسة مجهولة حقيقية على **كل** جداول المدرسة:

| الهجوم | النتيجة |
|---|---|
| قراءة `schools` و`school_members` و`classes` و`class_students` | ٠ صف |
| قراءة `teacher_files` و`teacher_links` و`teacher_exams` و`exam_assignments` | ٠ صف |
| قراءة `username_lookup` و`password_reset_log` و`job_runs` | ممنوع (الصلاحية مسحوبة) |
| ترقية نفسه إلى **إدارة** في `school_members` | رُفض بالسياسة |
| إنشاء فصل / رفع ملف / إنشاء اختبار بانتحال معلّم | رُفض بالسياسة |
| حذف كل ملفات المدرسة / كل أعضائها | ٠ صف |
| تعديل كل عناوين الملفات | ٠ صف |
| الموافقة على طلب دخول شاشة | ٠ صف |
| زرع طلب حساب **موافَقٍ عليه سلفاً** | رُفض بالسياسة |
| `approve_account_request` (ترقية نفسه) | رُفض: `NOT_ADMIN` |
| `resolve_username_email_admin` (حصد البريد) | رُفض: `permission denied` |
| قراءة أو رفع ملفات في `teacher-files` و`exam-images` | ٠ من أصل ٢، والرفع مرفوض |

**الباب الوحيد المفتوح بالتصميم** هو إدراج طلب حساب — وقد صار محروساً (بند د).

### دوال SECURITY DEFINER

راجعتُ كل دالة يستطيع `anon` أو `authenticated` تنفيذها:

- `link_my_school_account` — تطابق بالبريد **المُتحقَّق منه في `auth.users`**؛
  والمستخدم المجهول بلا بريد فلا يطابق شيئاً. سليمة.
- `move_students_to_class` — إدارة + تأكيد Google + الطلاب من نفس المدرسة
  ودورهم طالب. معرّفات المتصفح لا يُوثق بها، وهي لا تثق بها. سليمة.
- `approve_account_request` — إدارة + تأكيد Google + الطلب معلّق + له بريد.
  تُنشئ العضوية وتُحدّث الطلب في معاملة واحدة. سليمة.
- `screen_login_approve` — تسجيل دخول + تأكيد Google للمعلّم/الإدارة + مطابقة
  الرقم + غير منتهٍ ولا مستعمل + القفل الجديد. سليمة.
- `get_admin_analytics` و`get_visit_stats` و`pin_forum_post` — تفحص `app_admins`
  أو الملكية داخلياً. سليمة.
- `my_*` و`i_*` و`is_school_admin` — مساعِدات السياسات، ولا بدّ أن ينفّذها
  `authenticated` لأن تعبير السياسة يُقيَّم بصلاحية القارئ. كلها إمّا مقصورة
  على السائل نفسه أو تُرجع قيمة منطقية عن معرّف يجب أن يعرفه أصلاً.

### جداول بلا سياسات — **مقصودة، وموثّقة هنا عمداً**

`screen_login_requests` و`email_send_log` و`gemini_rate_limit` و`job_runs`
و`username_lookup` و`password_reset_log`.

لا يمسّها المتصفح إطلاقاً؛ كلها عبر دوال Netlify بمفتاح الخدمة. و`RLS` مفعّل
بلا سياسات = منعٌ تام. **وزيادةً في الحيطة سُحبت صلاحياتها أيضاً** من `anon`
و`authenticated`: لو أضاف أحدٌ سياسةً يوماً بالخطأ، تبقى الصلاحية المسحوبة
تمنع. مُدقّق Supabase سيظل يذكرها بمستوى `INFO` — وهذا متوقَّع ومقصود، لا خلل.

---

## ٣) ما يحتاج قراراً منك (دقيقة واحدة في لوحة Supabase)

**تفعيل الحماية من كلمات المرور المسرّبة** — مجاني، وبضغطة واحدة:

> Supabase → Authentication → Policies → **Leaked password protection** → تفعيل

يمنع Supabase عندها أي كلمة مرور ظهرت في تسريبات معروفة (عبر HaveIBeenPwned).
وأثره حقيقي: أكثر اختراقات حسابات الطلاب سببها كلمة مرور مستعملة في موقع آخر
سُرّب. ولا يمسّ هذا حسابات Google إطلاقاً (لا كلمة مرور فيها أصلاً).

---

## ٤) كيف تُعيد تشغيل الفحص

```bash
node tools/فحص-دوال-الخادم.js   # يشغّل دوال Netlify فعلاً — ١١ اختباراً
node tools/تدقيق-الدمج.js       # بقايا الدمج والكود الميت
```

واختبارات قاعدة البيانات تُشغَّل عبر Supabase مباشرة (محاكاة جلسة ضيف/طالب/
معلّم/إدارة بضبط `role` و`request.jwt.claims`).

⚠️ **وتحذير من تجربتي في هذا الفحص نفسه:** ثلاثة من اختباراتي الأولى مرّت
"بنجاح" وهي فارغة — كانت تستعمل أسماء أعمدة أو مستودعات غير موجودة، فيفشل
الهجوم لسبب لا علاقة له بالحماية. **الاختبار الذي ينجح دائماً لا يفحص شيئاً.**
فتحقّق دوماً أن الاختبار يسقط حين تُعيد الخلل عمداً — كما في:

```bash
# نُعيد خلل username عمداً ونتأكد أن الفحص يمسكه
sed -i 's/await getDisplayName(user)/username/' netlify/functions/send-email.js
node tools/فحص-دوال-الخادم.js   # يجب أن يسقط
git checkout netlify/functions/send-email.js
```
